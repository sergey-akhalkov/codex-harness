# Subscription model roles

The subscription-routing lifecycle connects this directory to the native Codex
user agent directory only while the corresponding proxy integration is installed.
Each role declares its model explicitly. Credentials belong to the host's
OpenCodex credential store, never to a role file.
