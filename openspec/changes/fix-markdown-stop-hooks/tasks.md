## 1. Correction

- [x] 1.1 Fix Markdown local dependency access; reproduce the reported document and pass real sibling-link, broken-link, changed-target and resource-boundary checks.
- [x] 1.2 Make Stop delivery idempotent and successful clearance nonblocking; pass repeated/concurrent handler, active-stop, failure and new-revision regressions with real journals.
- [x] 1.3 Recognize completed native failures without rerunning them in the companion; verify later source/configuration/registry changes still trigger reconciliation and existing timeout/progress tests pass.

## 2. Global acceptance

- [x] 2.1 Verify linked global entry points outside this checkout, including real Markdown diagnostics and a fresh native Codex edit/error/correction run without duplicate completion or clean blocking.
- [x] 2.2 Record the user request, causes, evidence and running-session activation boundary; validate OpenSpec and changed documentation links.
