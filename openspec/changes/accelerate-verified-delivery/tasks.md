## 1. Freeze comparable acceptance inputs

Scope clarification, 2026-09-08: no further opencode-kit run or ongoing support
is required. Completed evidence is retained. Tasks 5.3, 5.4 and 6.4 remain open;
the remaining quantitative acceptance scope is awaiting the user's decision.

- [x] 1.1 Define the eight outcome cases from the design with exact inputs, correctness oracles, allowed effects, environment/budget, cache policy and predeclared practical-effect criteria; verify every case maps to a specification requirement and that no criterion depends on candidate results.
- [x] 1.2 Preserve reproducible pre-repair diagnostic source/executable identity and controlled workloads without touching the active diagnostic change; verify the unchanged-tree, single-edit and concurrent-work baselines can be reconstructed from the recorded inputs.
- [x] 1.3 Prepare isolated recorded states of `opencode-kit` and the second real consumer, confirm their native command prerequisites and select the concrete external process regression; verify source checkouts and live services remain unchanged, commands have explicit knowledge states, and the regression has a fixed original failure condition.

## 2. Deliver the focused skill sources

- [x] 2.1 Create `project-verification` with bounded discovery, project-native commands, real entry-point/build identification and focused-then-required validation; verify skill structure and references with the applicable skill-creation checks and exercise the stale-executable example through its actual entry point.
- [x] 2.2 Add concise command-record guidance and examples using the consuming project's existing documentation home; verify confirmed/docs-only/unknown/blocked states, relevant-input invalidation, historical-success limits and interrupted-task continuation against concrete records.
- [x] 2.3 Create `reproduce-regression` with pinned identities, explicit oracle limits, failure-preserving reduction and scoped evidence; verify its examples reject a smaller case with the wrong failure and retain attempts for an intermittent or blocked reproduction.
- [x] 2.4 Supply only the reusable process resources needed by actual cases, reusing native helpers where applicable; verify concurrent output beyond pipe buffers, observable readiness failure, natural exit versus forced timeout, bounded cleanup and survival of unrelated processes/files with executable fixture checks.

## 3. Extend outcome execution and reporting

- [x] 3.1 Add the minimal case runner/reuse boundary around `tests/agent-delegation.py` and `tools/delegation-usage.py`; verify isolated native configurations expose the intended skill set in each arm, preserve current provider/model policy and prevent baseline contamination from links or previous case records.
- [x] 3.2 Record complete attempt timing, first useful signal, correctness, interventions, retries/children and available usage with detailed traces outside tracked sources; verify deterministic records preserve failure/incompletion, include rework, avoid double-counting overlapping elapsed time and represent missing usage as unknown.
- [x] 3.3 Encode the eight case inputs and their independent outcome checks, including positive and negative activation, stale state, wrong executable, regression reduction, process failure and missing prerequisite; verify the deterministic oracles reject intentionally incorrect outcomes before native agent runs.
- [x] 3.4 Provide explicit case selection and concise evidence reporting; verify a selected subset runs without the full model suite, no tool/documentation hook triggers model evaluation, and reports retain every attempted run with excluded-comparison reasons.

## 4. Verify the linked installation lifecycle

- [x] 4.1 Connect both skills and referenced resources through existing source-linked discovery and reconciliation; verify fresh-session registration and actual resource access with the installed CLI without copied skill bodies or prompt-injected source paths.
- [x] 4.2 Extend applicable isolated lifecycle checks for skill/resource updates, spaces/Unicode paths, foreign collisions, disconnect, rollback and reconnect; verify only owned links change and source files plus unrelated user configuration survive.

## 5. Establish measured outcome acceptance

- [x] 5.1 Consume the relevant completed checks and outside-project acceptance evidence from `stabilize-diagnostic-reconciliation`; verify its tested source/runtime identity and required finding-delivery, clearance and incomplete-state behavior before using it as the shared hook version. Do not close this task while that acceptance is incomplete.
- [x] 5.2 Run the controlled hook before/after workloads with fixed skills and equivalent inputs; verify correctness alongside latency, preserve unsuccessful runs and report hook effects separately from skill effects.
- [ ] 5.3 Run one native baseline/candidate pair for every outcome case on the same accepted capability selection (currently hooks-off); verify actual task outputs, skill discovery/use, required checks and all allowed-effect invariants, preserving every attempt and any correction cost. Historical hook versions do not authorize global restoration.
- [ ] 5.4 Repeat the cases used to support benefit at least twice per arm across the two real consumers with alternating order; verify the predeclared improvement criterion and unchanged correctness, report variation, and correct/retest affected failures. Leave benefit acceptance open if results remain inconclusive or harmful.

## 6. Activate, demonstrate consumption and document the result

- [x] 6.1 Activate the accepted skills through the normal global installation lifecycle and verify a fresh ordinary Codex session outside the harness discovers both skills and their resources; preserve rollback evidence and existing sessions/state.
- [x] 6.2 Exercise project verification in two real external projects and regression reproduction in at least one after global activation; verify actual commands and outcomes in isolated recorded project state, preserve live checkouts, and distinguish any lint-only evidence from application correctness.
- [x] 6.3 Update installation/discovery documentation, the documentation route and a concise outcome evidence report with actual source identities, commands, results, limitations and conditional follow-ups; verify local links and ensure confirmed project decisions are distinguished from implementation observations.
- [ ] 6.4 Run applicable final checks and strict OpenSpec validation, review every requirement against delivered evidence and reconcile all task states; verify global delivery, demonstrated benefit and the diagnostic dependency are complete before marking this change implemented. Do not treat planning completion as implementation acceptance.
