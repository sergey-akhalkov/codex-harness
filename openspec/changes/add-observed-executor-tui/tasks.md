## 1. Qualify native attachment on the target CLI

- [ ] 1.1 Requalify `native_named_empty_thread_can_be_attached` and `native_two_clients_reconnect_tool_result_and_tui` in `crates/codex-harness/tests/task_control_contract.rs` against the explicit target native executable using the existing canned Responses fixture; retain version/build identity and evidence of zero assignment requests before attachment, exact-thread event delivery and one tool effect. Resolve any version-specific attachment failure before dependent integration.

## 2. Deliver the first complete managed TUI run

- [ ] 2.1 Extend the existing executor host/control route in `crates/codex-harness/src/executor_cli.rs` and `crates/codex-harness/src/executor_control.rs` to prepare the bound thread, attach its owned native frontend and submit the assignment once, reusing applicable facilities in `crates/harness-core/src/task_control.rs`; prove the actual spawn/run/watch path shows native messages and tool activity, retains its final answer, returns the existing watch result and automatically ends its owned surface. Give terminal I/O exclusively to the TUI and verify controller output does not corrupt it.
- [ ] 2.2 Make this presentation the default without changing `--exec` assignment input or existing command arguments; make explicit TUI mode observable and qualify native inline presentation for the supported explicit exec contract. Remove replaced renderer code and tests only after equivalent coverage; retain only a proven necessary compatibility adapter on the same lifecycle. Extend `crates/codex-harness/tests/executor_spawn.rs` and `crates/codex-harness/tests/executor_observation.rs` to verify defaults, both presentation spellings, legacy receipt handling, configured model/provider/effort, cwd, single-agent restrictions, and an actionable attachment failure before any assignment model request.
- [ ] 2.3 Integrate automatic frontend/backend cleanup with the terminal-host policy owned by `close-failed-executor-tab`, preserving its final-message recovery and real receipt exit status; verify successful and unsuccessful results survive tab closure, an owned console retains its run exit semantics, and a cleanup failure names surviving resources without altering the recorded assignment outcome.

## 3. Preserve control, continuation and failure semantics

- [ ] 3.1 Correlate accepted turns and terminal events in `crates/codex-harness/src/executor_control.rs` and `crates/codex-harness/src/executor_observation.rs`; extend the existing control/observation tests to cover stale completion on resume, native TUI input and addressed corrections at a turn boundary, persisted final results, empty-final output defects, disconnection, lost host, timeout without stopping and repeated watch after timeout. Verify input is either delivered once or honestly reported undelivered when closure has begun. Preserve unresolved reply holds when the messaging workflow is integrated: its waiting result is nonterminal, the TUI stays open, and one reply continues the same run before normal closure.
- [ ] 3.2 Preserve `executor message` and urgent `executor stop` with the frontend attached through their existing owners; verify native delivery to the exact thread and prompt interruption/owned-process cleanup using `crates/codex-harness/tests/executor_message.rs` and `crates/codex-harness/tests/executor_stop.rs`, including an active tool, partial files and a neighboring run that remains unaffected.
- [ ] 3.3 Handle unexpected frontend exit in the existing executor host and process owner; verify no new model request follows view loss, partial work and session identity survive, watch reports interruption or the specific observed failure, and a completion/closure race cannot overwrite a retained result or fabricate success. Verify an unselected or unfocused tab remains a valid surface.
- [ ] 3.4 Move pooled exact-session resume onto the managed presentation and retain fresh-session restart semantics in `crates/codex-harness/src/executor_cli.rs`; extend existing executor continuation checks to verify exact session and slot retention, new-session/predecessor identity on restart, no reset or replay of completed work, and no stale control endpoint affecting the successor.
- [ ] 3.5 Verify the integrated route retains cache-loss interruption, executor shell selection and owned process/CPU admission using the applicable existing executor cache, shell and process checks; correct regressions in their existing owners without changing model routing or protection policy.

## 4. Accept the native entrypoint and deliver it globally

- [ ] 4.1 Extend the existing native executor acceptance setup with two isolated runs using actual Codex TUI/app-server and canned responses; exercise the real command sequence through successful completion, message, stop, manual TUI closure and exact-session resume. Verify one assignment/tool effect, readable native content, identity and result agreement, automatic closure and neighboring-run survival. Keep terminal/process observations and raw evidence local; do not substitute protocol mocks or conversation screenshots for this acceptance.
- [ ] 4.2 Run the affected project-native format, diagnostics, Rust tests and executable-ownership checks from `docs/rust-native.md`, using the established verification workflow; record the exact exercised build and remaining limits, and require all specified native scenarios to pass before claiming implementation complete.
- [ ] 4.3 Update `docs/agent-delegation.md`, `docs/rust-native.md`, the owning decision in `docs/project-decisions.md`, and affected text in `.agents/skills/team-lead/SKILL.md` to describe the delivered default, watch compatibility, automatic closure and recovery. Replace obsolete procedural text instead of appending another mode manual; account for generated/help/reference text under `simplify-native-harness`. Verify local links, source hygiene and examples, and reconcile the base lifecycle with additive messaging requirements without changing externally maintained OpenSpec workflow files.
- [ ] 4.4 Deploy through the existing global installation lifecycle and run the installed spawn/watch/control/continuation path in an owned workspace outside the kit checkout; verify native TUI, binding, result persistence, closure and recovery against the installed binaries, with existing running sessions and unrelated settings preserved. Use model-free native acceptance for deterministic proof; distinguish it from subscription availability and retain any authorized real-consumer evidence in its local owner.
