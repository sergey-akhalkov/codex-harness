## 1. Shared CPU owner and first working path

- [x] 1.1 Extend the existing Rust process owner in `crates/harness-core/src/process.rs` with a CPU-only shared account job and ordered outer/inner admission, preserving exclusive named-job and kill-on-close behavior. Verify simultaneous first joins, shared membership, 75% readback, name/ownership conflicts and independent lifecycle cleanup in `crates/harness-core/tests/process.rs` with owned native fixtures.
- [x] 1.2 Connect the ordinary installed Codex bootstrap in `crates/harness-core/src/native_launcher.rs` and `crates/codex-harness/src/bin/codex.rs` to that owner before payload execution. Verify one direct executable plus grandchildren from an outside-checkout synthetic consumer, measure actual CPU use, and preserve arguments, cwd, streams, exit status and ordinary interactive input in `crates/codex-harness/tests/native_launcher.rs`. This is the first end-to-end result, not completion of aggregate coverage.
- [x] 1.3 Implement warned fail-open behavior in the same installed bootstrap, including unavailable checkout and CPU setup/readback failure. Verify a fake upstream payload starts exactly once, receives unchanged native inputs, emits a visible diagnostic without stdout contamination, leaves peer caps intact, and does not require a build/update or persist a disabled default; extend the existing native-launcher acceptance cases.

## 2. Complete agent and service coverage

- [x] 2.1 Connect task-control, executor, resume and terminal-dispatch routes through `crates/harness-core/src/task_runtime.rs` and the existing executor owners under `crates/codex-harness/src`. Extend `crates/codex-harness/tests/task_control_launch.rs` and applicable executor tests to show that independently started sessions share the same group while unrelated terminal tabs remain outside it.
- [x] 2.3 Admit kit-owned shared MCP/backends through `crates/harness-core/src/process_service.rs`, `process_service_wmi.rs` and `broker_launch.rs`, including sibling starts outside ordinary inheritance. Extend `crates/codex-harness/tests/process_service.rs` and `broker_service.rs`; verify real retained MCP calls, group membership and backend survival when one of two clients exits. Validate both launch-time admission and reporting/restart of pre-existing unadmitted services.

## 3. Batch interoperability, exceptions and status

- [x] 3.1 Compose `crates/harness-core/src/heavy_command.rs` and `crates/codex-harness/src/heavy_command_cli.rs` with the shared owner. Preserve queue, memory, timeout, cancellation and exit-code contracts; reconcile the old default CPU policy and translate intentional lower limits to their documented host-relative meaning. Verify direct and nested heavy calls, standalone native builds, invalid/copied membership markers and absence of accidental percentage multiplication or admission deadlock in `crates/codex-harness/tests/heavy_command.rs`.
- [x] 3.2 Add an explicit per-invocation uncapped selector for sessions and commands using the existing independent service/bootstrap launch owner. Verify an exception requested from inside an already capped session actually escapes only the CPU group for that invocation, preserves lifecycle/console behavior, leaves peers and shared services capped, and does not change subsequent defaults. Keep exception launch work bounded and reuse existing local transport and identity checks.
- [x] 3.3 Add installed budget/coverage inspection and concise startup diagnostics in the existing manager/check surfaces. Verify machine-local policy ownership, actual membership/effective rates, explicit exceptions, warned degraded starts, unknown members and pending restart reporting; status must not mutate policy, run workloads or call a model, and MCP/native stdout protocols must remain intact.

## 4. Global activation and practical acceptance

- [x] 4.1 Integrate Codex connections, policy migration, safe restart reporting and rollback with the existing installation/update lifecycle. Verify preview and repeated installation, independent upstream updates, different agent homes, retained active sessions, ambiguous legacy policy and later user edits. Activation must remain incomplete until old uncovered sessions/services have safely transitioned; do not kill work to clear the status.
- [ ] 4.2 Run the installed aggregate acceptance matrix from outside this checkout: two concurrent project/session trees, both supported agents, direct native descendants, shared MCP work, nested managed builds, explicit exceptions and warned admission failure. Use synthetic Rust workloads and a representative longer build/test run; retain actual host logical-processor count, duration, non-double-counted aggregate CPU time and kernel membership/settings through the existing local verification lifecycle. Declare sampling bounds first, investigate any persistent quota/measurement discrepancy, and verify useful interactive input and cancellation under load before claiming enforcement.
- [ ] 4.3 Exercise normal exit, abrupt session/control-owner death, reconnect, incomplete activation and rollback while peer work remains active. Verify the common allowance is neither duplicated nor silently lifted and unrelated sessions/services retain their documented lifecycle; reuse the existing process, service and native-launcher acceptance owners.
- [ ] 4.4 Reconcile `docs/rust-native.md`, `docs/installation.md` and `docs/project-decisions.md` with the delivered commands, 75% aggregate policy, explicit uncapped invocation, warned fail-open behavior, coverage/restart limits and preserved batch safeguards. Verify examples against installed help and use only synthetic identities and paths; do not retain private incident data or introduce another policy/status mirror.
- [ ] 4.5 Complete applicable locked Rust formatting/check/clippy/tests under the existing capped native verification route, then run `openspec validate add-shared-agent-cpu-budget --strict --no-interactive`, `openspec validate --all --strict --no-interactive`, scoped source/publication checks and `git diff --check`. Close implementation tasks only on their actual evidence; planning completion and kernel setting readback alone are not runtime acceptance.
