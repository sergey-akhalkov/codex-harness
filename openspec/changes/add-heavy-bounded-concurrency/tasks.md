## 1. Core admission and aggregate envelope

- [ ] 1.1 Replace single-lease heavy admission with a bounded slot set in `crates/harness-core/src/resource_admission.rs`: per-slot lock files, try-any acquisition under the existing bounded poll and queue wait, per-slot holder descriptions, and unchanged exclusive behavior for every other resource. Verify in the module's tests that slots within the bound are held concurrently, a caller beyond the bound waits and then fails at queue-wait expiry without disturbing holders, dropped or cancelled leases release exactly their slot, and the legacy `heavy-command.lock` name is no longer taken for admission.
- [ ] 1.2 Add the account aggregate Job owner to `crates/harness-core/src/process.rs`: named per account, created or opened by each caller, commit-memory limit only (no CPU rate), handle-held lifetime, membership and limit snapshot. Verify with owned native fixtures that two independent processes join one kernel object, readback reports the configured limit and zero CPU rate, the object closes with the last handle, and name or ownership conflicts fail closed with a clear error.
- [ ] 1.3 Compose `crates/harness-core/src/heavy_command.rs` with slots and the aggregate envelope: policy parsing and validation for `max_concurrent_trees` (positive; 1 restores serialized admission) and `aggregate_memory_limit_bytes`, documented defaults for missing fields, admission order slot -> aggregate Job -> shared CPU budget -> payload, per-tree containment Job kept inner, and nested heavy admission that inherits the outer admission instead of taking a second slot. Verify direct and nested runs, aggregate and per-tree readback in run diagnostics, invalid policy values, unchanged exit codes, and the documented no-deadlock admission order.

## 2. CLI, policy and status surfaces

- [ ] 2.1 Update `crates/codex-harness/src/heavy_command_cli.rs` and its tests: `heavy budget` text and JSON report slot count, aggregate memory, per-tree memory and each field's source; the queue diagnostic prints one bounded line with busy and total slots plus available holder descriptions; `--account` isolation covers both slots and the envelope; reads never rewrite legacy policy bytes; installed `heavy --help` and `heavy budget --help` match the implementation.
- [ ] 2.2 Extend every existing manager or check surface that already reports heavy budget fields with the new effective values, keeping native stdout protocols intact and adding no new policy mirror. Verify through the existing check-surface tests that status is query-only, reports degraded or unknown admission honestly, and does not start work or mutate policy.

## 3. Documentation

- [ ] 3.1 Reconcile `docs/rust-native.md` and `docs/installation.md` with bounded heavy concurrency: slot policy and defaults, aggregate memory envelope, queue diagnostics, serialized rollback via slot count 1, mixed-version rollout note, and unchanged shared CPU ceiling. Record the design decision in `docs/project-decisions.md`. Verify examples against installed help using only synthetic identities and paths.

## 4. Acceptance and gates

- [ ] 4.1 Run the installed acceptance from outside this checkout: two concurrent representative Rust build or test trees in two synthetic checkouts under one account, a third caller waiting with the slot diagnostic, cancellation of one admitted tree releasing its slot and cleaning up only its tree, a nested heavy call inside an admitted tree, a legacy policy file left byte-identical, and slot count 1 restoring serialization. Declare sampling bounds first and retain kernel membership, aggregate memory and aggregate CPU evidence through the existing local verification lifecycle before claiming enforcement.
- [ ] 4.2 Complete the applicable locked Rust formatting, check, clippy and workspace tests under the existing capped native verification route, then run `openspec validate add-heavy-bounded-concurrency --strict --no-interactive`, `openspec validate --all --strict --no-interactive`, scoped source and publication checks, and `git diff --check`. Close implementation tasks only on their actual evidence.