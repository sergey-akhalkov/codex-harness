## 1. Fix the comparison boundary and remove isolated duplication

- [x] 1.1 Record the source revision/dirty inputs, explicit Codex/RTK versions and production/test/instruction baseline defined in `design.md` using the existing verification owner; fix equivalent direct-work, dispatch, reply and recovery inputs. Verify installed help and one existing small native contract check, distinguishing interface availability from proven substitution. Evidence: clean baseline 6e262c9 with Codex 0.157.0, RTK 0.48.0 and installed build cb8c5f775cdb013d; local `simplify-native-harness/baseline/baseline.json` records production/test Rust and four fixed instruction scenarios; installed executor/lead help and the exact-profile launcher contract test pass. Two retained harness-observe receipts show owner-PowerShell launch failure inside its observation job, so the baseline script ran directly in the owner shell.
- [ ] 1.2 Trace production, test, configuration, supported external and active-change consumers of `task_orchestrate::plan_executors`/old `steer`, `skill-evolution` catalogue/delivery helpers and benefit-gate/pacing evaluators. Record each represented rule's retained consumer or unfinished task; verify that recovery, CPU admission and publication/awareness consumers are not accidentally classified as unused. Do not delete solely because a symbol search finds only tests.
- [ ] 1.3 Remove verified unused helpers and implementation-specific tests in those exact owners, preserving equivalent rule coverage and required work. Verify affected crate tests, compilation/caller checks and ownership accounting; report production reduction separately from tests and retain any unresolved external compatibility case.

## 2. Deliver native configuration and discovery paths

- [ ] 2.1 Simplify effort selection in `crates/harness-core/src/launcher.rs` and affected launcher consumers to canonical native configuration with only necessary legacy translation. Verify native/profile override precedence, invalid legacy inputs, remote and configured executor routes, unchanged default/model/provider and actual effective effort through existing launcher checks. Update the corresponding owned effort examples without removing the public alias.
- [ ] 2.2 Use native effective skill discovery through the existing native-read facilities for the relevant consumers in `crates/skill-evolution` and `crates/codex-harness/src/skills_cli.rs`; retain only missing identity/coverage metadata. Verify duplicate links, conflicting names, disabled/missing skills, scoped discovery, truncation and unavailable-native reporting. Reuse the current native skill-session checks for turn/compact/resume/child behavior; do not equate reload with awareness or remove usage/publication safeguards.
- [ ] 2.3 Replace redundant tool-list filtering in `crates/harness-core/src/serena_stdio.rs` through supported settings in the existing Serena/MCP configuration owners, retaining only proven necessary adaptation. Verify installed initialize/activate guidance and accepted tools, representative Rust/Python semantic operations, honest diagnostics coverage, two-client project isolation and one client's exit without peer/service damage using existing broker/MCP checks.

## 3. Consolidate the integrated executor path

- [ ] 3.1 Consume the working native-TUI and lead-messaging owner changes with their completed native acceptance; preserve the failed-tab, cache-loss, current CPU and xAI-startup guarantees. Verify an actual spawn/watch/message/reply/stop/resume path with one conversation identity and no duplicate dispatch, renderer, transcript or delivery engine. Keep this dependency incomplete if either required feature has not been delivered.
- [ ] 3.2 Compare native shared-daemon and existing isolated-backend ownership in `crates/codex-harness/tests/task_control_contract.rs` with the existing synthetic provider. Verify pre-request visible attachment, exact profiles, active/idle delivery, reconnect, CPU membership and neighbor survival on stop/view loss. Adopt and integrate the simpler equivalent route, or retain the isolated route with the precise observed gap; do not create another daemon or change everyday setup.
- [ ] 3.3 Consolidate overlapping executor/task-controller mechanics in their existing `executor_*`, `task_control`, `task_runtime`, `task_store`, `task_view` and recovery owners. Remove only replaced paths, preserving accepted succession work, bounded pool/release rules, process ownership and supported legacy receipts. Verify stopped-work recovery, pending reply holds, native-turn versus run completion, empty-final defects, exact-session continuation and configured provider binding through the real entrypoints.

## 4. Resolve remaining native replacement candidates

- [ ] 4.1 Compare installed `codex doctor --json` with existing `source_diagnostics*` and `native_read_rpc` observations for one healthy and one owned broken-input case. Remove only redundant general-health logic/calls, retaining native config/skills provenance and kit links/build/ownership findings; verify incomplete/error classification, read-only behavior and no model calls. Record retention if doctor supplies no cheaper equivalent fact.
- [ ] 4.2 Qualify installed native RTK Codex rewriting against `tools/rtk-adapter/src/main.rs` and `token_workflow_lifecycle.rs` on PowerShell 7, including literal arguments, stderr, nonzero exit, unsupported/machine output and one actual execution. Replace equivalent mechanics and verify raw retention, digest-checked paged recall, bounds and hook lifecycle; retain only missing guarantees with an observed reason.
- [ ] 4.3 Remove proven duplicate package/config mechanics in existing `dependency_*`, installation, registration and build owners using their native `uv`/package-manager/CLI interfaces. Verify retained candidate integrity, transaction interruption, later user edits, direct source links, active immutable builds and disconnect/recovery through the affected existing checks. Retain plugin/migration adapters where the native lifecycle is not equivalent; do not retire compatibility based on age alone.
- [ ] 4.4 Qualify each existing xAI wire adaptation using the target CLI and current contract in the existing shim/native transport tests; remove proven obsolete rewrites only. Verify OAuth helper use, cold-start preparation from `fix-xai-executor-shim-startup`, concurrent service reuse and one executor's exit without peer transport loss. A complete direct-route replacement additionally requires authorized real-provider evidence; otherwise record retention without weakening the route or probing another billing source.
- [ ] 4.5 Reconcile test-only feedback/pacing/gate logic with the real `feedback_cli.rs` and `board_feedback.rs` consumers and the existing orchestration/skill-evaluation requirements. Use native `bd` primitives and one consumed implementation of each required rule; verify distinct votes, duplicate/uncertain retry, threshold and consequence routes, partial promotion recovery, missing budget evidence and required evaluation gates. Remove unused duplicates without adding manual calculations or a second tracker.
- [ ] 4.6 Consolidate repeated process/result mechanics across `structured.rs`, `regression.rs`, `verification_record.rs` and `outcome_*` only where current consumers share the same contract. Verify schema-valid but incorrect output, stale result, timeout/partial output, changed input, process cleanup and existing outcome comparisons; retain independent oracles, token attribution, disk-reclaim and ownership boundaries. Record a no-change decision for a candidate when extraction adds more machinery than it removes.

## 5. Reduce instructions and prove the combined result

- [ ] 5.1 Shorten the affected text in `global/principles-of-work.md`, `global/harness.config.toml`, `.agents/skills/team-lead/SKILL.md`, `.agents/skills/board-workflow/SKILL.md`, relevant owned skills/references and generated executor briefs. Keep one home per operational fact and remove mechanics now guaranteed by commands; verify model/authority/visibility/board/acceptance decisions remain explicit and protected OpenSpec workflow files are unchanged.
- [ ] 5.2 Reconcile affected CLI help and `docs/agent-delegation.md`, `docs/rust-native.md`, `docs/token-workflow.md`, `docs/code-tools.md`, `docs/source-diagnostics.md`, installation/subscription guides and existing decision records with delivered behavior. Remove obsolete parallel-command guidance and completed narratives only after preserving needed operation/recovery facts; verify installed syntax, local links, source hygiene and no duplicated status/configuration owner.
- [ ] 5.3 Compare the complete final code and instruction surfaces with task 1.1: require lower production code and lower combined owned instruction bytes/words, separately report tests/fixtures/docs, and require no instruction-load growth in any fixed scenario. Include new references, metadata, briefs and mandatory help in the accounting; reject reductions obtained by moving/minifying text, dropping checks or adding recurring manual work.
- [ ] 5.4 Exercise the shortened direct-work, dispatch, clarification/reply and interrupted recovery guidance through actual installed components. Reuse required native acceptance and authorized real consuming-project work for model-use evidence; retain the existing skill-evaluation checks when applicability/behavior changes. Verify correct command selection and complete accepted outcomes, and do not substitute a text-lint or synthetic-provider result for actual instruction-use acceptance.

## 6. Deliver and reconcile all owners

- [ ] 6.1 Build and run the affected locked native checks, formatting, explicit diagnostics and executable-ownership checks through `docs/rust-native.md`; retain exact build identity and honest diagnostic freshness. Reuse unchanged accepted tests, add only checks needed for changed behavior, and verify every design-table candidate has a removal or evidence-based retention decision.
- [ ] 6.2 Deploy through the existing immutable global lifecycle and verify the affected installed commands in an owned consumer outside this checkout, with concurrent sessions and required failure/recovery cases. Preserve current sessions, partial work, provider/auth state, CPU policy and unrelated settings; exercise rollback/disconnect only on owned acceptance targets and confirm the final restored state.
- [ ] 6.3 Reconcile proposal, design, delta/main-spec composition and active-change ownership against observed results; validate with `openspec validate --all --strict --no-interactive` and `git diff --check`, plus applicable link/publication checks. Preserve all TUI, waiting/reply, closure, cache, CPU and xAI scenarios, do not overwrite another change's requirement on archive, and close tasks only on their own execution evidence.
