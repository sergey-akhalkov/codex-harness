## 1. Подготовить сопоставимые входы и область реализации

- [ ] 1.1 Сверить текущие source/runtime/dirty identities, владельцев после Rust-миграции и действующий состав MCP, skills, hooks и языков; подготовить только нужные инструменты в фактическом checkout. Проверка: сохранён компактный manifest, representative navigation и небольшой applicable check работают, ограничения coverage отражены; чужие изменения и selected services сохранены.
- [ ] 1.2 Подготовить owned inputs и независимые oracle для C1/C2/S1/S2/O1/M1/M2/B1/G1/Q1/A1/E1/L1 из design; до первых сравнений записать materiality/regression tolerances, budget и условия повторов. Проверка: критерии различают неверный ответ, потерянную ошибку и меньшую выдачу; точные baseline/candidate inputs и allowed effects доступны для воспроизведения без новой evaluation framework; допуски исключают материальное ухудшение скорости.

## 2. Доставить основные маршруты инструментов

- [ ] 2.1 Дополнить существующий token-efficient workflow маршрутами CBM для diff impact, scoped architecture, value/call tracing и semantic discovery; сохранить index freshness/coverage и fallback. Проверка C1/C2: actual operations находят известных consumers/relationships, entry-point oracle проходит, stale/partial/unsupported случаи не превращаются в отрицательные доказательства; unchanged generation переиспользуется.
- [ ] 2.2 Уточнить Serena routes, подсказки и выбранные operation boundaries через supported configuration или короткую owning инструкцию; сохранить подходящие native edits и проектные проверки. Проверка S1/S2: реальный read/edit/caller сценарий ловит введённый дефект; effective tools и owned negative writes подтверждают заявленную границу, пустая диагностика не выдаётся за clean; существующая и новая сессии различаются явно.
- [ ] 2.3 Описать и применить Nuphus scoped snapshots и batch до смысловой развилки; добавить маршрут matching Apps с resource/session identity. Проверка B1/A1: DOM oracle подтверждает эффект и продолжение после частичного сбоя без слепого replay; доступная App выполняет read, отсутствующий ресурс остаётся явным negative result; внешние записи не выполняются.
- [ ] 2.4 Доставить bounded Code Mode retrieval/result shaping в существующем workflow, не создавая обязательный wrapper на каждый вызов. Проверка O1: success, rejected promise, MCP error, nonzero exit и oversized result остаются различимы; нет двойной выдачи одного payload, детали доступны без повторного выполнения; истёкший locator отмечается как отсутствующий.

## 3. Сравнить и выбрать интерфейс Git-памяти

- [ ] 3.1 Проверить нужные prerequisites `git-project-memory` и подготовить native/Serena candidate в независимом owned Git root без дублирования владельцев. Проверка M1: одинаковые решения, stale-command и no-new-knowledge задачи дают правильный результат, нерелевантные тела не читаются, правило актуальности и текущая инструкция пользователя сохраняются; неподтверждённые prerequisites не отмечены завершёнными.
- [ ] 3.2 Упражнить topic access, rename, read-only/ignored patterns, оба формата ссылок и частичный отказ записи на owned памяти. Проверка M2: защищённые данные не изменены, неподдержанные ссылки выявлены, partial state и recovery наблюдаемы, unrelated edits сохранены; успех Serena не считается доказательством атомарности.
- [ ] 3.3 Проверить выбранную структуру записей в fresh clone без личной истории и Serena, а также конфликт двух worktrees и смену пользовательского решения. Проверка M2: ignore/tracked состояние и actual clone contents подтверждают переносимость; untracked/ignored запись не выдаётся за доставленную, runtime cache не попал в fixture commit; native entry route ведёт к единственным владельцам, обе конфликтующие версии и evidence сохраняются до интеграции; решение обновлено без второй авторитетной копии.
- [ ] 3.4 По одинаковым M1/M2 задачам сравнить стоимость интерфейсов и зафиксировать `adopted` scope либо `retain-native`; отразить выбор в существующем project-memory workflow. Проверка: rationale соответствует заранее заданным критериям, проверки корректности/переносимости выполнены, нет sync service, обязательной глобальной памяти или повторного onboarding существующих records; окончательное native сравнение включено в 6.1.

## 4. Оценить дополнительные возможности

- [ ] 4.1 Выполнить G1 для связи code/config/docs: проверить installed extraction/query/update path, known answer и стоимость подготовки относительно CBM + прямого чтения. Проверка: graph identity и fresh-source update подтверждены; `adopted` требует реального построения/обновления, `retain-current` содержит конкретную причину непригодности/невыгодности доступного пути; нет скрытого model/API вызова или скачивания.
- [ ] 4.2 Выполнить Q1 для Serena cross-project querying: сверить штатный Project Server contract, ресурсные лимиты и сравнить с существующим broker/activation на двух roots. Проверка: одинаковые имена не смешиваются; при допустимом owned запуске измерены процессы/cleanup, при раннем отклонении сохранено конкретное доказательство несовместимости; чужие workers не затронуты.
- [ ] 4.3 Встроить результаты G1/Q1 в короткие маршруты и существующий lifecycle только для принятых вариантов. Проверка: каждый кандидат имеет terminal selection и доступный baseline; для нового выбранного процесса есть проверенный ownership/cleanup/rollback, иначе он не поставляется; отклонение кандидатов не закрывает обязательные C/S/O/M/B/L проверки.

## 5. Согласовать owning документы и инструкции

- [x] 5.1 Сверить связанные OpenSpec artifacts памяти, skill evolution и verified delivery; scoped правками устранить противоречащие hooks-off assumptions, сохранив все согласованные outcomes и честные незакрытые задачи. Проверка: diff показывает сохранение требований, unsupported compact/resume delivery остаётся owning blocker, остановленное пользователем исследование не возобновлено, другие change не объявлены завершёнными; применимая strict validation проходит.
- [ ] 5.2 Свести подробные маршруты к существующим skills/references и коротким глобальным entry points, согласовать `memory_maintenance` и проектные правила. Проверка: relevant task обнаруживает нужный маршрут, unrelated task не загружает весь каталог/всю память, инструкции не копируются между владельцами, ordinary diagnostic/context/Stop hooks и CBM auto index/watch остаются выключенными.

## 6. Подтвердить качество и оценить полную стоимость

- [ ] 6.1 Выполнить native baseline/candidate эпизоды code, memory и browser на зафиксированных inputs; покрыть связанные строки матрицы одним эпизодом где возможно. Проверка E1: каждый actual entry-point oracle и meaningful failure path пройден, одинаковые model/effort и исходные inputs подтверждены; setup, работа родителя/child, verification, failures и correction cost сохранены. Положительный выбор по эффективности и повторяемые benefit claims поддержаны минимум двумя чередующимися парами; дополнительные runs имеют конкретное основание.
- [ ] 6.2 Сопоставить результаты с predeclared tolerances и принять только подтверждённые улучшения; устранить обнаруженные P0/P1 и перепроверить затронутые сценарии. Проверка: нет post-hoc ослабления критериев, проигравшие/inconclusive кандидаты сохраняют baseline, bytes/estimated tokens/measured tokens/time/quota различены, неполные обязательные проверки остаются открытыми; findings reviewed по исходному evidence.

## 7. Глобальная поставка и завершение

- [ ] 7.1 Подключить принятые маршруты/настройки через текущий kit installer и проверить обычный native entry point в двух независимых owned Git roots вне harness, включая подходящий named tool-capable child. Проверка L1: native prompt input подтверждён в обоих roots, один содержит локальные AGENTS; actual guidance discovery и применение поддержаны tool results с правильными roots; policy не объявлена scheduler, project-local MCP registration и ручная подгрузка вместо штатного discovery не используются как заменитель глобальной поставки.
- [ ] 7.2 Проверить install/update, перенос kit, disconnect и rollback в isolated lifecycle roots, затем реальную глобальную активацию. Проверка L1: kit-owned ссылки/настройки корректны, project memories и чужие edits/settings/shared installations остаются целыми, rejected candidates не оставили служб; модельные маршруты, billing и hook/resource selection сохранены.
- [ ] 7.3 Обновить owning usage/installation/evidence документы и краткую запись решения, сопоставить каждый requirement/scenario с текущим evidence и задачами. Проверка: local links, applicable checks и `openspec validate improve-installed-tool-workflows --strict` проходят; все обязательные сценарии и условные решения закрыты по фактам, глобальное потребление подтверждено. Planning status не считается реализацией; архивирование, commit и push автоматически не выполняются.
