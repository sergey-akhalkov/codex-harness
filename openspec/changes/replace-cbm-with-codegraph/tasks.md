## 1. Planning evidence and instructions

- [x] 1.1 Qualify the pinned published package in isolation and compare actual retrieval on the pack and locally selected large repository. Verify release checksum, Job memory/deadline/cleanup, initial index and unchanged sync, exact symbols, direct callers, cross-file exploration, absent/ambiguous names and high fan-out; record scope and byte/latency measurements in design without private inputs.
- [x] 1.2 Verify bounded fixture watcher and connect-time catch-up, including a real post-index source addition; record observed refresh and cleanup separately from unperformed large-change/resource-failure acceptance.
- [x] 1.3 Deliver the measured task-based retrieval instructions through their existing global/skill links. Verify link targets, local document links and strict OpenSpec validation; distinguish instruction delivery from unimplemented runtime enforcement and unchanged MCP registration.
- [x] 1.4 Reconcile the Rust migration's proposal, design, requirements and tasks with this replacement. Verify a single Rust adapter owner, an explicit non-circular dependency/order map, no remaining instruction to continue the CBM port, preserved large-project/resource/recovery acceptance and strict validation of both changes; runtime tasks remain open.

## 2. Native provider and resource boundary

- [x] 2.1 Add the published CodeGraph package/identity to dependency discovery and recoverable installation sources. Verify checksum failure, already-installed adoption, read-only Check and no download/toolchain/interactive-installer side effects on normal startup.
- [x] 2.2 Connect CodeGraph through the existing Rust MCP and ownership primitives with per-client canonical roots, shared same-project resources and one account-wide heavy-indexing admission slot. Keep all active projects observed; verify fair different-root service across Codex homes, same-root index/watcher/backend reuse, bounded event queues, no detached upstream daemon, independent client disconnect/idle retirement and zero surviving owned descendants. Measure owned process counts and aggregate memory for one client, additional same-root clients and distinct projects; do not infer reuse from configuration.
- [x] 2.3 Enable watcher and connect-time incremental refresh in every simultaneously active indexed project under the same 2 GiB, one parse/one resolve worker, 25% CPU and finite operation limits. Establish actual pending/start/completion or finite sync boundaries, release admission between episodes and retain queued changes across healthy worker retirement. Verify add/change bursts/rename/delete across at least three roots without queries or manual sync triggering the refresh, service beyond 600 seconds, reconnect catch-up, pending-source fallback, memory/deadline failure, cancellation, per-project failure isolation and no automatic crash/restart loop. A finite worker lease alone no longer satisfies this task.
- [x] 2.4 Preserve a usable committed index across failed full rebuilds and automatic refresh. Verify native transaction behavior with forced failure; implement bounded staging/recovery where required and prove partial generations are not marked current.
- [x] 2.5 Enforce owned database/WAL/staging/detail storage limits and free-space reserve. Verify pressure failure and recovery without filling the drive, no repeated full copies on queries, bounded retention and link-safe cleanup that preserves product files and unrelated state.
- [x] 2.6 Verify Rust ownership of all new first-party CodeGraph integration and executable acceptance code, including provider lifecycle, response handling and generated/embedded programs. Inspect source, registrations and the exercised process chain; record published CodeGraph/Node as external, permit existing transitional lifecycle dispatch only, and reject owned foreign-language bridges before activation.

## 3. Retrieval contract and comparative acceptance

- [x] 3.1 Deliver a small stable tool catalogue and accurate initialization instructions, including small-repository handler discovery and explicit index/status. Verify real calls in indexed/unindexed roots, qualifier mismatch and hidden-handler differences against the pinned runtime; unsupported operations must be explicit.
- [x] 3.2 Enforce five-result/depth-one defaults, opt-in exploration, validated request ranges and 4 KiB serialized answers with an explicit 16 KiB maximum. Verify UTF-8, overloaded-name fan-out, long source lines/bodies, duplicate text/structured payloads, malformed/oversized frames, failures and warning retention through the actual managed MCP.
- [x] 3.3 Add bounded detail retrieval without rerunning backend queries; verify page limits, local ownership, expiry, accurate partial-source/list notices and no cross-client response or deduplication leakage.
- [x] 3.4 Compare raw and managed answers using identical source oracles on both real roots. Include lookup/body, overview, direct callers, impact/flow, false-edge, ambiguous, missing and fan-out tasks; account for startup/catalogue, follow-ups and latency. Verify useful byte reduction and retained answers, not just fewer calls or hidden information; report tokenizer/quota measurements only when actually made.
- [x] 3.5 Complete current large-repository acceptance with maintained-source inventory, full index, representative graph/text queries and automatic/manual refresh, including use alongside other concurrently active projects. Exercise changed/renamed/deleted files on an owned source snapshot or owned probe, preserve original product sources, and distinguish full-source acceptance from the initial no-change trial and small watcher fixture. Reuse still-applicable source oracles; recheck changed scheduler/generation paths rather than treat the single-project candidate as concurrent acceptance.

## 4. Reconciliation, delivery and recovery

- [x] 4.1 Apply and recheck the already recorded Rust ownership/dependency map against implemented source, tests, manifests and registrations. Verify the same native adapter/evidence is adopted by Rust task 5.4, CBM-only paths have a consumer-backed compatibility/retirement disposition, broader workflow comparisons retain their owner and selected runtime agrees with the plan; planning reconciliation alone does not close this task or any provider/global acceptance.
- [ ] 4.2 Activate the replacement through Install/Update/Recover/Disconnect and retire only the owned CBM registration after acceptance passes. Verify interrupted activation, user-edited configuration, rollback to CBM's manual policy, preserved CBM indexes/shared packages and no unrelated settings changes.
- [ ] 4.3 Verify current global instructions and actual managed operations outside the checkout in fresh interactive/non-interactive and supported resumed/child consumers. Through the installed global configuration keep at least three Codex CLI projects open simultaneously, add another same-project client, and verify queries and automatic updates in every root without closing the first session. Check client sharing, last-client retirement, unaffected remaining projects and automatic catch-up after reopening. Record restart boundaries and any unsupported consumer; a one-active-project restriction is a failed acceptance, not an operating condition.
- [ ] 4.4 Run applicable native checks and strict OpenSpec validation on the final combined changes; confirm all required tasks have current evidence before archiving. Keep private evidence and consumer paths outside Git and report remaining limits without claiming weekly subscription savings.
