## 1. Verify the native delivery contract

- [x] 1.1 Extend `crates/codex-harness/tests/task_control_contract.rs` with its existing owned Responses fixture to compare installed `codex queue` and native app-server input on the exact owning lead thread: active generation, a running tool including an executor observation wait, and idle waiting. Verify visible/native input evidence and subsequent provider input, record the installed version and selected route in this design, and do not equate help or RPC acceptance with delivery.
- [x] 1.2 Exercise native lead identity/endpoint discovery and caller attribution through the actual executor shell path, including two leads sharing a checkout and explicit profiles. Verify the normal installed launcher and remote attachment preserve settings and arguments; identify any required minimal launcher integration without activating window layout, starting a second copy of a live thread or building a new messaging service.

## 2. Deliver a minimal native request and reply

- [x] 2.1 Extend the existing dispatch/run identity and environment propagation in `crates/codex-harness/src/executor_cli.rs` and its process/control owners with the verified originating lead and immutable run context. Verify ordinary spawned descendants succeed, cwd changes are harmless, copied markers/references and stale or sibling processes fail, and spawn refuses an unverifiable lead channel before a model request.
- [x] 2.2 Implement `codex-harness lead message` in the CLI command owner using the selected existing native transport, `--text`/UTF-8 `--file`, default reply-request semantics and optional `--notify`. Verify a minimal actual-native question reaches its exact lead with literal multiline/Unicode content and harness-owned sender/worktree metadata, without exposing credentials or accepting recipient overrides.
- [x] 2.3 Extend `crates/codex-harness/src/executor_message.rs` with `--reply-to MESSAGE_ID`, the compact lifecycle-owned lookup and correlated reply delivery. Verify a minimal native round trip needs only the message reference and text, rejects another lead/conflicting addresses/retired references, and preserves the existing fully addressed command behavior.

## 3. Preserve waiting and delivery correctness

- [x] 3.1 Update the executor control host and observation lifecycle to retain the server, thread, visible surface, lease and worktree after a turn ends with unresolved requests. Verify a blocked executor stays reply-capable without model calls, a reply continues the same idle thread without resume, independent work can continue, and `--notify` alone does not prevent normal completion. Compose with the native-TUI and failed-tab-close owners: waiting keeps the owned frontend open, while actual run end retains their normal closure and outcome semantics.
- [x] 3.2 Expose waiting-for-reply and bounded unresolved message references through the existing watch/receipt/terminal owners. Make active and newly invoked watch return action-required exit 3 in text/JSON while preserving meanings 0/1/2 and the live run. Verify prompt return without waiting for timeout, waiting/timeout races, one-command reply followed by the same watch command, and no completion, output-defect, provider-unavailability, resume or slot-release claim from waiting.
- [x] 3.3 Reuse the existing message-attempt and native-input evidence owners to distinguish queued, delivered, refused, unavailable and indeterminate outcomes. Verify uncertain retries cannot double-deliver, identical text is allowed in a later resolved exchange, partial replies leave other requests unresolved, pending requests cannot be silently evicted, and definite send failure leaves no phantom hold.
- [x] 3.4 Add targeted native integration coverage for question-versus-turn-completion, reply-versus-turn-completion and reply-versus-stop races; lead disconnection/reconnection to the same thread; visibility loss; native server failure; stale sender/reply references after resume/restart and slot reuse. Verify exact identity, no silent reparenting or replacement conversation, preserved partial work and existing urgent-stop behavior.

## 4. Deliver concise instructions and compatibility

- [x] 4.1 Update both free-text and structured assignment rendering in `crates/codex-harness/src/executor_assignment.rs` and `executor_cli.rs`, including supported continuation guidance. Verify rendered briefs show the minimal commands, automatic waiting and notification distinction, appropriate clarification boundaries and bd ownership without manual address discovery, polling rituals or re-enabled native agent tools.
- [x] 4.2 Update CLI usage/help, `.agents/skills/team-lead/SKILL.md`, `docs/agent-delegation.md` and `docs/rust-native.md` in their existing homes. Verify source/installed command syntax, watch exit 3 and local links; replace end-to-escalate and manual-addressing instructions while retaining board-first progress, explicit stop/recovery and the prohibition on routine message nudges. Measure the combined generated/skill/reference/help instruction load under `simplify-native-harness`; do not hide growth by moving text between those surfaces.
- [x] 4.3 Verify legacy receipts, explicit executor message/stop commands and ordinary launcher degradation retain their supported behavior. Run affected native checks for spawn, message, observation, control, stop and assignment rendering, plus applicable formatting, source diagnostics and executable ownership checks; report unavailable/stale diagnostics separately from executed tests.

## 5. Verify global consumption and recovery

- [x] 5.1 Deploy the immutable native build and updated instruction owners through the existing harness lifecycle. Verify installed help/version agree with the source and a fresh consumer outside this checkout discovers the commands without copying kit files, endpoint setup or altered unrelated settings; preserve the prior build's recovery path.
- [x] 5.2 Run an installed two-lead isolation scenario using actual native sessions and owned synthetic provider inputs: each lead spawns its own executor, questions carry the correct worktree/session metadata, a blocked executor remains waiting, and one-command replies reach only the correct original runs. Verify visible surfaces, unchanged effective routing, no model polling, no resume command and no terminal rearrangement.
- [x] 5.3 Exercise the complete installed workflow with a real consuming-project assignment and its bd issue under the configured visible executor route: executor independently discovers an actual clarification need, sends the question, waits, consumes the lead's answer, continues the same work and records the durable decision/result on bd. Verify both model-visible inputs and resulting work; use the owning native checks for stop, preserved work and installation recovery. Retain private evidence locally and do not substitute model-free transport checks for this consumer acceptance.
- [x] 5.4 Reconcile the implementation, owning docs and these delta specs against the observed native route and integrated `add-observed-executor-tui` / `close-failed-executor-tab` requirements. Keep TUI's full base lifecycle requirement and this change's additive waiting observation as distinct delta operations, preserving both in combined-spec validation. Run strict OpenSpec validation and close tasks only with their actual evidence. Leave any failed acceptance explicitly unfinished; do not archive before global consumption and required recovery checks pass.
