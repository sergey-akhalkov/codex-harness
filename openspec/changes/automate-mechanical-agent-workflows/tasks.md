## 1. Instruction invariants

- [x] 1.1 Enforce principles size and shared report-owner routes in the native source checker; verify oversized, missing-owner and valid-source cases.
- [x] 1.2 Reduce principles below 24 KiB preserving normative rules; verify size, requirement mapping and native source hygiene.

## 2. Bounded token reports

- [x] 2.1 Implement bounded report/findings presentation and retained detail access with explicit expiry; verify full JSON/baselines, omitted counts, warnings and no-rescan detail reads.
- [x] 2.2 Correct owner routes through one shared declaration; verify every owner resolves in the current source.

## 3. Structured executor assignments

- [x] 3.1 Add validated assignment-file spawn/resume input and generated checkout/base brief; verify missing inputs, traversal/link escapes, new outputs and free-text compatibility without model calls.

## 4. Verification records

- [x] 4.1 Extend harness-observe with selected input/provenance and executable identity capture; verify success, changed input, missing input, failure, timeout and non-Git cases through the native entry point.

## 5. Feedback command

- [ ] 5.1 Connect existing bd algorithms to installed feedback commands; verify read-only inspection, repeated/diagnostic votes, configured bounds, promotion and partial failure recovery using synthetic boards.

## 6. Integration and global delivery

- [x] 6.1 Update owning docs and workflow guidance for all five paths, preserving protected external workflows; verify links, instruction limits and CLI examples.
- [ ] 6.2 Integrate executor commits and run affected native regression suites, formatting, clippy, ownership and source checks; retain failure evidence and resolve introduced defects.
- [ ] 6.3 Deploy the immutable build and exercise all five installed paths from outside the checkout with synthetic inputs; verify global links and receipt identities.
- [ ] 6.4 Reconcile specifications and close accepted board tasks; report measured output/step changes without claiming unmeasured token or quota savings.
