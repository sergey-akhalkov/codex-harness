## 1. Проверить конкретные runtime-контракты

- [x] 1.1 В owned probe проверить установленный Codex CLI и его схемы: обнаружение созданного/обновлённого скилла, additionalContext для выбранных событий, SessionStart startup/resume/compact, включая automatic mid-turn continuation. Переиспользовать consumers/ConPTY из `tests/`; сохранить версии, фактические события и ограничения. Завершение: определён и продемонстрирован поддержанный путь доставки в текущую CLI-сессию; отдельный App Server или restart-only результат не закрывает задачу.
- [ ] 1.2 Проверить предложенную публикацию на Windows: целый новый пакет, атомарное переключение descriptor и неизменяемые пути ресурсов существующего пакета, сбой между шагами и rollback. Завершение: failure-injection probe показывает только целостные используемые ревизии через live source link и сохраняет чужие изменения; результат определяет конкретный примитив публикации.
- [ ] 1.3 Выбрать и проверить существующий способ исполнения eval в изолированном owned target с недоступными для записи baseline/oracles/control files. Завершение: кандидат, пытающийся изменить oracle или выйти за test target, не может получить успешный verdict; зафиксированы runtime dependencies и пределы изоляции без изменений прав основной сессии.

## 2. Отбор опыта и авторство

- [ ] 2.1 Используя применимый `skill-creator`, создать глобальный `skill-evolution` с режимами create/update/consolidate/retire и общей политикой владения. Поставить короткий глобальный маршрут к workflow, не переопределяя встроенный creator. Завершение: проверки примеров различают полезную процедуру, факт, гипотезу, существующий инструмент и routine/read-only no-op; creator пишет только в переданный staging.
- [ ] 2.2 Реализовать хранение candidate/evidence вне discovery с переиспользованием существующей Git-памяти проекта; идентифицировать worktree, package/parent hashes и значимые зависимости. Завершение: дубликаты не размножаются, секреты и сырые transcripts не попадают в пакет/отчёт, отсутствует auto stage/commit/push, стабильный незакоммиченный пакет поддерживается.
- [ ] 2.3 Реализовать определение владельца, существующих совпадений и защищённых областей: control policy, evaluator, сам evolution/creator, third-party/system skills и explicit disablement. Завершение: проверки collision, symlink escape, попытки самоизменения и внешней инструкции сохраняют запрещённые targets; обычный owned предметный skill остаётся доступен для разрешённого развития.
- [ ] 2.4 Добавить signal-driven запуск и ограничение эпизода по объявленному бюджету/условию остановки. Завершение: без нового сигнала нет отдельного model run, одинаковая неудача без новых данных не перезапускает цикл, timeout/quota/unavailable оставляют pending и прежнюю версию без повторных запросов ревью пользователю.

## 3. Оценка полезности

- [ ] 3.1 Реализовать сравнения new/no-skill, update/old-version и consolidation/old-set в свежих контекстах и сопоставимых окружениях. Завершение: исполняются подходящий, похожий неподходящий и boundary/failure cases с отдельным неиспользованным при refinement вариантом; авторская переписка не передаётся evaluator.
- [ ] 3.2 Подключить детерминированные project/artifact checks, проверку фактического чтения/применения скилла и защищённую фиксированную rubric для необходимой субъективной оценки. Завершение: skill bypass, запись в oracle/result/baseline и ложный self-report не проходят; revision/dependency drift инвалидирует затронутое evidence.
- [ ] 3.3 Реализовать verdict по must-pass условиям, заявленной пользе, регрессиям и заранее объявленной политике неопределённости; сохранить provider/model/revision/input identity и полный измеренный расход эпизода. Завершение: более быстрый кандидат с пропущенной обязательной проверкой отклоняется; неоднозначный результат pending; смена провайдера не смешивает стороны сравнения и все OpenAI-вызовы остаются Astra.

## 4. Обратимая публикация и обслуживание

- [ ] 4.1 Реализовать публикацию проверенного package hash из staging в owning project source по результату 1.2: compare-and-swap baseline, короткая блокировка, целостная ревизия, безопасные canonical paths. Завершение: concurrent writers, изменение ресурса после eval и прерывание multi-file update не дают принять смешанную или непроверенную версию.
- [ ] 4.2 Реализовать journal/recovery/rollback с проверкой текущего владения перед восстановлением и раздельным статусом accepted, published и delivered. Завершение: незавершённая операция восстанавливается после перезапуска helper, foreign edit не затирается, ссылка не используется для рекурсивного удаления её цели, неполное восстановление имеет точную причину.
- [ ] 4.3 Реализовать promotion в canonical harness после проверки origin и второго независимого проекта, очистки локальных assumptions/secrets и проверки dependencies. Завершение: переносимый кандидат публикуется через отдельную глобальную операцию, непереносимый остаётся local/pending, пересекающиеся локальные и глобальные идентичности согласуются без потери проектного поведения или незакоммиченной работы.
- [ ] 4.4 Реализовать consolidation/retirement и удержание нужной истории ресурсов вне обнаружения retired skills. Завершение: replacement проходит прежние сценарии обоих навыков, известные ссылки согласованы, редкое использование/неизвестный dynamic consumer не приводят к удалению, откат возвращает предыдущий пригодный пакет.

## 5. Актуальные навыки в сессии

- [ ] 5.1 Реализовать производный scoped catalogue с canonical identity, applicability, path/revision, disablement, дедупликацией одной цели ссылки и конфликтами разных sources. Завершение: candidates/retired/чужие project skills исключены; каталог восстанавливается после удаления runtime state; overflow явно даёт маршрут к не поместившимся записям, а не ложный полный список.
- [ ] 5.2 Подключить явное чтение принятого descriptor и доставку revision delta в текущем turn, а также обработку изменения от другого writer перед следующим использованием. Завершение: обычная matching task без `$skill` использует новую ревизию; update/rollback не оставляет старые инструкции для следующего use; повторные unchanged events не вставляют полный каталог.
- [ ] 5.3 Реализовать восстановление через подтверждённые в 1.1 SessionStart compact/resume механизмы и передачу релевантного path/revision детям. Завершение: manual и automatic mid-turn compaction восстанавливают актуальность до продолжения, resume учитывает disablement, ребёнок без fork и уже работающий ребёнок применяют нужную ревизию с честной атрибуцией прежних результатов.
- [ ] 5.4 Ограничить обработчики по времени/объёму, экранировать недоверенные поля каталога и сохранить существующие diagnostic handlers. Завершение: malformed metadata, injection text, timeout и неизвестная runtime capability дают ограниченный incomplete результат; нет рекурсивного обучения из hooks, Stop loops, потери diagnostics или model calls на каждом событии.

## 6. Подключение через lifecycle kit

- [ ] 6.1 Добавить scoped reconciliation только принятой skill-регистрации в существующий lifecycle с ownership/mutex/recovery. Завершение: новый общий навык подключается без ручного reinstall; busy/foreign destination сохраняются; unrelated config/dependencies/подписки/MCP не переустанавливаются и службы не перезапускаются.
- [ ] 6.2 Включить workflow, policy, runtime helpers и session handlers в `global/kit.psd1`, требуемые исходники и обычную установку/Check/Recover/Disconnect. Завершение: проверки installer подтверждают прямые связи без deployed body copies, идемпотентность, сохранение foreign hooks/skills и проектных записей при отключении.

## 7. Полная приёмка и глобальная поставка

- [ ] 7.1 Провести матрицу A/B/F из design в двух independent owned Git repo вне harness с различными реалистичными workflows: автономное извлечение из обычной задачи, исправление после использования, отбор, consolidation и retirement. Завершение: сохранены inputs, baseline/candidate results и независимые критерии; user prompts не подсказывают создавать скилл, fixture-статус проектов обозначен честно.
- [ ] 7.2 Провести матрицу C/D через реальный установленный CLI/ConPTY: применение в том же turn, следующая задача без `$skill`, update/rollback, manual и настоящий automatic mid-turn compact, resume и оба child-сценария. Завершение: trace/артефакты доказывают применение нужной ревизии, unrelated request не активирует скилл; App Server discovery и hook mocks не заменяют эти проверки.
- [ ] 7.3 Провести матрицу E и integrity-сценарии H: competing worktrees, half publication, foreign rollback edit, concurrent installer, sensitive evidence, poisoned candidate/oracle, provider unavailable и исчерпание бюджета. Завершение: действующая версия и чужая работа сохранены, неполные состояния честны и восстанавливаемы, прогоны не затрагивают управляющую сессию или production targets.
- [ ] 7.4 Замерить полный эпизод обучения и обычную работу без learning signal, доставку каталога, overflow и failure handlers по заранее заданным limits и comparison policy. Завершение: отчёт включает расходы на создание/проверку/переделки, объясняет полезность принятых изменений и неопределённость; отсутствие подтверждённого эффекта не скрывается количеством скиллов или токенов.
- [ ] 7.5 Выполнить lifecycle/relocation/fresh-clone приёмку G в isolated test roots, затем активировать реальный kit глобально и проверить workflow из harness и двух внешних repo. Завершение: подключённые исходники и accepted skill update видны через обычный entry point; нет test-only глобальных навыков, проектная память/скиллы переживают clone/disconnect, глобальная установка отделена в evidence от изолированных тестов.
- [ ] 7.6 Обновить owning документацию установки, применения, ограничений и отката, `docs/project-decisions.md` и карту документации; сопоставить все требования и сценарии с evidence. Завершение: локальные ссылки и `openspec validate autonomous-skill-evolution --strict` проходят, все обязательные сценарии выполнены, задачи закрыты по реальным результатам; недоступный required сценарий сохраняет change незавершённым. Архивирование, commit и push этим change автоматически не выполняются.
