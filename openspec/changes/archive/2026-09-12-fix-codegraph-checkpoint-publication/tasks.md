## 1. Reproduction and correction

- [x] 1.1 Reproduce installed startup failure, rebuild the source-stale manager and verify a real upstream MCP initialize response with owned registration recovery available.
- [x] 1.2 Pin the automatic refresh failure to its filesystem operation with a reduced native regression and retained installed-consumer diagnostics.
- [x] 1.3 Correct transient checkpoint sharing conflicts and verify success, cancellation/deadline and saved-checkpoint recovery through native generation tests.

## 2. Delivery

- [x] 2.1 Build and activate the corrected immutable manager through the existing registration/inventory lifecycle; verify healthy identity and unchanged retained MCP settings.
- [x] 2.2 Pass the installed three-project consumer index/query/automatic-refresh/reopen scenario outside the checkout, and complete applicable formatting, source/link and OpenSpec checks.

Verification: 14 native generation tests passed; the published three-project
scenario passed, followed by the installed native Codex scenario (50.77 seconds,
no model calls). Changed Rust files pass rustfmt. Source/link checks reported
zero findings. Full workspace formatting and strict Clippy remain limited by
pre-existing changes in unrelated task-control, lifecycle and routing files.
