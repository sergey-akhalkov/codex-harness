## 1. Preserve hook connections

- [x] 1.1 Retain the failing global command evidence and add installer regressions for core update, repeat, relocation, missing-link repair, conflict preservation and fresh core-only behavior; show failure before correction.
- [x] 1.2 Correct the desired link inventory and pass the installer lifecycle suite, preserving rollback and unrelated work.
- [x] 1.3 Pass the command hook's effective registry to its diagnostic child so it shares the native MCP runtime identity; verify the reproduced identity mismatch disappears and the installed error/correction and transport-race checks pass.

## 2. Restore and verify globally

- [x] 2.1 Restore links using the transactional installer after backup and preview; verify the identical formerly failing command, all command event types, real diagnostic error/correction and a subsequent core update from outside the repository.
- [x] 2.2 Record the confirmed user request, cause, verification and rollback; check modified PowerShell diagnostics, documentation links and strict OpenSpec validation, then close all tasks.
