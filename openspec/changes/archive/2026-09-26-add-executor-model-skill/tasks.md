- [x] 1. Switch the executor binding: kit `global/orchestration.toml` sets `executor_profiles = ["ds"]`; the same one-line switch is applied and verified in one consuming checkout. Evidence: each repository's diff changes exactly that line; readback prints the kit and consumer `executor_profiles` lines with `["ds"]`.
- [x] 2. Add `.agents/skills/executor-model` with the canonical one-action switch block and no executable file. Evidence: the block executed verbatim as extracted from `SKILL.md` printed the `deepseek-flash`/`max` binding and both checkout readbacks (exit 0); an uninstalled profile id refuses with the installed list; `quick_validate.py` reports "Skill is valid!"; `codex-harness ownership-check --source .` reports 0 findings.
- [x] 3. Deliver through the kit link mechanism instead of machine-local authoring: the separately authored personal copy is removed and `<user profile>/.agents/skills/executor-model` is a SymbolicLink to this checkout's skill directory, matching the other kit skills.
- [x] 4. Record the authoring rule in `AGENTS.md`: reusable capability is authored only in this repository; machine-local directories hold only lifecycle-produced links, builds and state.
- [x] 5. Sync the `xai`-pinning harness-core unit tests and the "currently `xai`" statements in `docs/agent-delegation.md` and `openspec/specs/agent-delegation/spec.md` to `ds`. Evidence: all 13 orchestration-filtered harness-core tests pass; `cargo fmt --all -- --check`, workspace clippy `-D warnings`, strict change validation, source diagnostics and ownership checks pass; no current executor-routing statement remains pinned to `xai`/Grok 4.7.
