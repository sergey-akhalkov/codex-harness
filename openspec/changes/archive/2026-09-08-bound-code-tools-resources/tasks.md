## 1. Incident and policy

- [x] 1.1 Preserve measured incident, installed identities and process/configuration baseline; record the confirmed exclusion of a common Codex server.
- [x] 1.2 Define reusable bounded resource policy and generated-input setup, preserving product sources and specifications.

## 2. Process ownership

- [x] 2.1 Implement atomic Windows job ownership with non-inheritable handles, aggregate memory/CPU limits and safe service lifetime.
- [x] 2.2 Verify normal exit, abrupt owner death, initialization failure and memory/CPU enforcement with owned descendant fixtures.

## 3. Codebase Memory

- [x] 3.1 Route delivered indexing through one account-wide admission slot, two workers, hard limits and a finite deadline; retain native query/tool contracts.
- [x] 3.2 Migrate auto-index/watch configuration recoverably, apply the pmac-emulator generated-log exclusion and expose explicit-refresh policy.
- [x] 3.3 Verify concurrent admission, failure preservation of committed indexes, timeout cleanup and a bounded real pmac-emulator index.

## 4. Shared language service

Historical checks below established the earlier broker implementation. The accepted `reduce-subscription-waste` selection now controls retention; these completed tasks do not authorize restoring diagnostic hooks, journals or Stop delivery. Retained services still require the ownership and isolation checks.

- [x] 4.1 Implement authenticated local broker startup, runtime identity checks, bounded backend cache and idle retirement.
- [x] 4.2 Route MCP and hook clients through compatible root/language/configuration backends while preserving per-client roots, journals, claims and revision checks.
- [x] 4.3 Verify same-project reuse, cross-project/configuration isolation, concurrent claims, client disconnect and service-crash cleanup with applicable native checks.

## 5. Other MCP processes

- [x] 5.1 Reuse compatible Serena project backends with per-client project routing, serialized operations and idle retirement; verify activation isolation and semantic calls.
- [x] 5.2 Retain verified Graphify endpoint reuse and bounded graph contexts; preserve lazy isolated Nuphus browser state, serialize desktop mutations and own child cleanup.
- [x] 5.3 Remove unnecessary resident launcher layers while preserving Windows STDIO, supported entrypoints and registration compatibility.

## 6. Global activation and acceptance

- [x] 6.1 Integrate policy and services into global install/update/rollback with preservation of later user edits; run applicable lifecycle checks.
- [x] 6.2 Activate globally, clean only freshly verified historical orphan trees and exercise real delivered MCP/LSP entrypoints outside the checkout.
- [x] 6.3 Measure idle/active memory and CPU, document evidence and recovery boundaries, and close every task only after its acceptance checks pass.
