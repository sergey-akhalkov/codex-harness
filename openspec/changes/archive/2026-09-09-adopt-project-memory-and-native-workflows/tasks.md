## 1. Память в Git проекта

- [x] 1.1 Создать глобальный `project-memory` skill и короткие шаблоны индекса/записи по design, используя `skill-creator`; проверить frontmatter, относительные ресурсы и отсутствие встроенных фактов конкретного проекта или отдельного model call для записи.
- [x] 1.2 Внедрить маршрут памяти в harness: связать `AGENTS.md`, короткий индекс и существующие решения/проверки без копирования авторитетных записей; проверить ссылки, выборочное чтение и сохранение посторонних изменений.
- [x] 1.3 Проверить workflow в owned независимом Git-проекте и свежем clone с новым Codex home: агент применяет известное решение, не читает нерелевантные тела, перепроверяет устаревшую команду и сохраняет один новый подтверждённый факт; сохранить фактические evidence и итоговый diff.
- [x] 1.4 Упражнить смену пользовательского решения, отсутствие полезной новой записи и конфликт памяти между двумя worktrees; проверить приоритет нового решения, отсутствие бессмысленных дописок и сохранность обеих версий при интеграции.

## 2. Контекст Astra и документирование работающего btw

- [x] 2.1 Выполнить ограниченный pilot `features.context_management.experimental_mode=true` в новой Astra задаче на существующей subscription route; отдельно зафиксировать parser/effective-setting/eligibility/runtime evidence и проверить применение раннего ограничения при продолжении. Если activation не установлена, сохранить точный отказ и оставить runtime приёмку открытой; не заменять её одним `features list`.
- [x] 2.2 При подтверждённом pilot подключить выбранную настройку через live профиль и существующий installation lifecycle; проверить новый процесс вне harness, явный override, откат в обычный контекст и сохранность остальных настроек, Goals, native memories и модельной маршрутизации.
- [x] 2.3 Добавить краткое руководство native side questions на основе уже полученных receipts и подтверждения пользователя; проверить соответствие prerequisites, aliases и return controls наблюдениям/официальному контракту, явно сохранить ограничения ConPTY-проверки. Не возобновлять закрытый разбор `/btw`, не включать v2 и не делать дополнительные model calls ради него.

## 3. Изолированные задачи через worktrees

- [x] 3.1 Создать глобальный `isolated-worktree` skill с подготовкой из `project-verification`, явными base/dirty inputs, критериями полезности изоляции, проверкой MCP checkout и правилами интеграции; проверить skill и наличие полного примера для Windows CLI без desktop app.
- [x] 3.2 Выполнить end-to-end задачу в worktree независимого fixture-репозитория: передать нужные tracked/untracked dirty inputs, проверить фактический root и свежесть используемого MCP, подготовить окружение, получить изменение и интегрировать его; приёмка — независимый ожидаемый результат и применимые проверки уже объединённого дерева.
- [x] 3.3 Проверить коллизию branch/path, конфликт интеграции, отказ проверки после merge и попытку очистить несохранённый worktree; убедиться, что чужие изменения, shared services и незавершённая работа сохранены, а удалены только безопасные owned ресурсы.

## 4. Структурированные запуски Codex

- [x] 4.1 Создать глобальный `structured-codex-run` skill, поддерживаемую native JSON Schema для ограниченного инспекционного результата и минимальную проверку результата с переиспользованием существующего process observer; проверить аргументы/stdin, явный sandbox, уникальный output и раздельные final JSON/event JSONL/process receipt.
- [x] 4.2 Упражнить детерминированные отрицательные случаи: auth/process failure, timeout, принудительное завершение, output limit, missing/malformed/stale JSON и schema-valid wrong answer; каждый должен дать свой честный отказ без ложного success и сохранить нужную partial evidence.
- [x] 4.3 Выполнить один реальный Astra `codex exec --output-schema` consumer вне harness на read-only fixture с независимым oracle; проверить natural exit, terminal task outcome, schema, правильность finding и неизменность исходников. Зафиксировать subscription/model route; для Grok не заявлять schema enforcement без отдельного фактического доказательства.

## 5. Глобальная поставка и приёмка

- [x] 5.1 Подключить новые навыки и их ресурсы существующим установщиком, меняя manifest/diagnostics только при необходимости; проверить discovery из другого проекта, прямые источники, обновление/повторную установку и сохранение unrelated capabilities.
- [x] 5.2 Проверить отключение и восстановление в owned consumer home: свои registrations и context override управляются корректно, project memory и пользовательские файлы остаются; применить итоговую поставку глобально и подтвердить её из нового процесса вне harness без остановки shared proxy.
- [x] 5.3 Свести результаты требований в компактный evidence-документ и обновить маршруты документации/решения; проверить OpenSpec, ссылки и применимые installation/consumer checks, записать реальные ограничения, время и переделки без неподтверждённых процентов экономии. Закрыть задачи только после всей соответствующей приёмки.
