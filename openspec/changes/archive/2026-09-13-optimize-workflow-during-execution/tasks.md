## 1. Preserve and prepare

- [x] 1.1 Preserve the touched dirty baseline privately, verify installed source-link identities and run the existing native source/link audit; distinguish pre-existing findings and unrelated work.
- [x] 1.2 Prepare the external native task and counterexamples defined in the design, establish independent expected content and initial CLI failure, and verify supported visible execution before model dispatch.

## 2. Implement in existing owners

- [x] 2.1 Replace overlapping global speed/feedback guidance with the execution-boundary trigger, discriminating next observation and immediate proportionate investment; review against all delta scenarios and preserved authority/acceptance constraints.
- [x] 2.2 Update the two skill routes and their existing feedback/session references for same-stage failures, live replay, reset/restart, cost measurement and useful delegation; run both skill format validators and check local links.
- [x] 2.3 Update the owning user-decision and global-instruction records without private consumer details or duplicated procedures; verify existing unrelated edits are preserved.

## 3. Exercise delivery and behavior

- [x] 3.1 Verify final source links, current global prompt loading and discovery/readability of both skills from the owned external consumer, keeping old-session loading unknown.
- [x] 3.2 Execute the independent visible external task on final relevant instructions; inspect actual tool choices and independently validate the resulting requirements, protected files, cheap correction and full project result. Retain actual elapsed cost and any failures privately.
- [x] 3.3 Exercise and review the bounded continuation counterexamples for uncertain interactions, valid/invalidated state, full-check necessity, pending restoration, reset/restart and delegation payback; correct failures and distinguish synthetic decisions from live application evidence.

## 4. Accept and archive

- [x] 4.1 Record concise observed results and limits in the existing global-instruction guide; complete final skill/source/link/whitespace and strict OpenSpec checks, with unchanged unrelated findings classified against baseline.
- [x] 4.2 Synchronize and verify both deltas without losing existing requirements/scenarios, complete the tasks, archive this change through the installed CLI, and rerun strict validation and the final scoped audit.
