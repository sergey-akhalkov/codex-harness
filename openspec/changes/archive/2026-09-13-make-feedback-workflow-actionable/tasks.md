## 1. Establish the scoped verification path

- [x] 1.1 Preserve the touched source baseline privately, confirm current source-linked owners and native check entry points, and execute the baseline source audit; retain actual findings and verify unrelated work remains outside the write scope.
- [x] 1.2 Prepare the bounded real external native task described in the design with owned inputs, an independently checkable parent result, actual installed-tool identity, session/effect boundaries and recorded initial acceptance; verify the prompt does not prescribe the desired change of method and that required visibility/resource isolation is usable before model execution.

## 2. Implement the feedback policy

- [x] 2.1 Consolidate the global speed/feedback instructions around the unresolved fact, smallest valid check, direct observed exploration, primary failure and separate restoration; verify necessary integration, ownership, explicit user choices and complete acceptance remain intact without adding a mandatory report or retry quota.
- [x] 2.2 Update the verification skill's activation description and feedback mechanics, and the token skill's session reuse/waiting mechanics; run the existing skill validators and review realistic positive and negative activation against their descriptions.
- [x] 2.3 Update the existing global-instruction and decision records for the delivered behavior, fresh-context limits and rollback; repair the directly related archived design links and verify local links plus preservation of private publication boundaries.

## 3. Verify delivery and actual behavior

- [x] 3.1 Verify final source-link identity, complete global instruction loading with local precedence and skill discovery/read access in a fresh supported consumer outside this checkout; retain native evidence and keep older-session loading unknown unless directly established.
- [x] 3.2 Run the prepared independent external native task using the final relevant policy inputs; inspect tool actions and product effects for successive driving/observation difficulties, a smaller feedback boundary and reuse, then independently verify the unchanged parent result and required recovery. Retain failures and all elapsed costs; a narrated success is insufficient.
- [x] 3.3 Exercise the remaining bounded counterexamples for necessary full integration, cheap direct work, action/observer disagreement, pending supervisor/restoration and invalidated or resumed state. Verify actual effects where executable, clearly scope simulated decisions, and reject a promised method change that does not reach the next applicable actions.

## 4. Reconcile and archive

- [x] 4.1 Reconcile both deltas into their main specifications with the current specs instructions, retaining every unrelated requirement/scenario and the previously accepted adaptive contract; compare every selected delta after merging and verify strict main-spec validation.
- [x] 4.2 Record concise current operating conditions, observed outcomes, full costs and evidence limits in the owning guide; run strict change validation, the native source/link audit and scoped whitespace/publication review, classifying any unchanged unrelated findings against the baseline.
- [x] 4.3 Reconcile every requirement and task with actual evidence, then archive through the OpenSpec workflow with both deltas verified synchronized; confirm the archive is present, the change is absent from active changes and final links resolve after relocation.
