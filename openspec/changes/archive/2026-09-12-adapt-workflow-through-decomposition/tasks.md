## 1. Establish the first adaptive path

- [x] 1.1 Preserve the relevant current principles, skill and guide inputs as a private pre-change baseline; select a bounded authorized real external task with repeated preparation or slow feedback, and fix its initial inputs, unchanged acceptance and the design's comparison criteria before execution. Verify that the baseline is replayable or has complete matched observations, and that unrelated dirty work and private consumer data remain protected.
- [x] 1.2 Adapt the existing speed/feedback and collaboration paragraphs in `global/principles-of-work.md` to require event-driven reassessment and independently verifiable decomposition before local cycle optimization. Verify that different errors in one unresolved layer trigger reassessment, a first informative failure can suffice, and small direct work and necessary full integration retain their short valid paths without new approval or retry quotas.
- [x] 1.3 Extend `.agents/skills/project-verification/SKILL.md` with preparation/driver/product/observation failure attribution and extraction of the smallest meaningful feedback boundary. Verify that the original failure, real dependencies and all mandatory parent checks remain intact, using the existing regression skill only within its current scope.
- [x] 1.4 Check the first changed sources through the native source/link audit and a fresh external consumer using the supported linked lifecycle. Verify the exact revised principles and verification skill are discoverable before relying on them in the real task; distinguish loading from behavior.

## 2. Deliver and measure one useful result

- [x] 2.1 Exercise the selected real task under the revised policy through the existing native session/probe route, explicitly opting into any model-backed probe. Verify that the agent identifies the actual bottleneck without a decomposition hint, completes an independently checked result, integrates it through the parent entry point, preserves ownership/restoration and passes the task's unchanged acceptance. This is the first useful end-to-end increment; incomplete parent checks remain open.
- [x] 2.2 Compare that run with the preserved matched baseline using total elapsed time, first useful signal, expensive preparation/full-run counts, coordination, interventions and rework. Verify the design's practical improvement criterion with full costs and unchanged correctness; correct observed overhead or failed behavior, and retain an unfinished result if the comparison is unavailable or inconclusive.

## 3. Complete reusable workflow guidance

- [x] 3.1 Extend `.agents/skills/token-efficient-workflow/SKILL.md` with cost-based reuse of preparation/sessions, affected-input invalidation and preservation of useful knowledge across interruption. Verify that reuse requires valid conditions, checks stay scoped to changes, and speculative automation or unsupported savings claims are not introduced.
- [x] 3.2 Update `docs/agent-delegation.md` with the concise workstream result/input/dependency/check/consumer handoff, one-owner runtime rules, independent parent work and integration/restoration closure. Verify that research-only assignments and direct execution remain valid, and that role selection, concurrency, aggregate resource limits and authorization are preserved.
- [x] 3.3 Reconcile the changed guidance with the existing `reproduce-regression`, `isolated-worktree` and project-memory owners. Verify consistent reuse and failure/recovery semantics without duplicating their procedures, broadening their triggers, editing external OpenSpec workflows or introducing a new scheduler or skill catalogue.

## 4. Verify the complete behavior and external delivery

- [x] 4.1 Exercise design case A's successive different failures with the final relevant policy inputs, reusing the real-task run when it covers this case and remains applicable. Verify that the agent isolates the common layer before another dependent full run and that the parent actually consumes the result; rerun only evidence affected by later edits.
- [x] 4.2 Exercise design cases B, C and D through bounded native agent tasks. Verify rejection of a reduction that loses the failure, direct completion of a small edit, and preservation of an irreducible full integration check; assess actions and outcomes rather than recitation of the rules.
- [x] 4.3 Exercise design cases E and F with owned synthetic resources. Verify single-owner interaction, useful independent work within resource limits, explicit pending integration, recovery after an interrupted handoff, revalidation after changed inputs or an ended session, and reuse of unaffected findings. Verify that a bounded research answer can complete its assigned decision without an unrelated executable artifact.
- [x] 4.4 Verify final global initial-context loading from two external directories, including a repository with project instructions, and relevant skill discovery through fresh supported sessions. Verify correct source links, preserved project overrides/unrelated capabilities, and the documented fresh-session/rollback behavior without disturbing the active control channel.

## 5. Close evidence and documentation honestly

- [x] 5.1 Record the confirmed adaptive-workflow decision in `docs/project-decisions.md` and update the existing delegation/global-instruction operation guides only for current operating conditions and scoped evidence. Verify private consumer identities, paths and raw observations stay outside shared source and that no duplicate session report is added.
- [x] 5.2 Run applicable skill-format checks, `openspec validate adapt-workflow-through-decomposition --strict --no-interactive`, applicable all-spec validation, the current native source/link audit and a scoped diff/whitespace review. Verify no new findings originate in this change; preserve and explicitly classify unrelated pre-existing failures instead of modifying their scope or claiming a clean global result.
- [x] 5.3 Reconcile both delta specifications and every task against current loading, behavior, integration, recovery and comparison evidence. Verify full accepted scope is delivered, unresolved checks or regressions remain open, and claims distinguish observed improvements from unmeasured long-term benefits and subscription savings.
