## 1. First working subscription path

- [x] 1.1 Acquire the pinned OpenCodex package, inspect installed operational contracts and record current Codex/OpenCode connection identities; verify version, source identity and redacted baseline.
- [x] 1.2 Configure an isolated loopback probe and complete separate xAI OAuth authorization; verify an authenticated account model list and a harmless Grok completion without changing OpenCode credentials.
- [x] 1.3 Resolve the observed login memory incident before another OpenCodex launch: bounded reproduction, browser-only login, Windows process-tree memory limit and timeout, and tested descendant cleanup; record observation separately from incident attribution.

## 2. Reusable global delivery

- [x] 2.1 Add non-secret source configuration and a fixed Grok reviewer role using the verified account model and reasoning; validate schemas and verify source contains no credentials.
- [x] 2.2 Implement proxy package, source-link, service and routing ownership with Install, Update, Check, Recover and Disconnect support; verify preview purity, idempotence, foreign-conflict refusal and recovery with focused lifecycle tests.
- [x] 2.3 Integrate the proxy component into the existing installer and transaction order; run relevant existing launcher/core/activation regressions and verify MCP rollback is not broken by proxy config writes.
- [x] 2.4 Activate the delivered integration globally through its installer, verify hidden background readiness, source-link identity and existing harness/MCP discovery from an external directory.

## 3. Actual consumer acceptance

- [x] 3.1 Run ordinary Codex with Grok as the main model outside this repository, exercise a harmless local tool and verify actual subscription provider/model in proxy evidence.
- [x] 3.2 Run GPT-6 Astra as parent and invoke the exact named Grok role outside this repository; verify unique task delivery, tool execution, selected provider/model and returned findings.
- [x] 3.3 Exercise unavailable-model/authentication failure without fallback or paid-API routing, distinguishing controlled fixtures from live subscription evidence.
- [x] 3.4 Verify repeat install, managed background restart, disconnect/native restore and reconnect with retained credentials; confirm OpenCode configuration/auth preservation and scope any reboot evidence accurately.

## 4. Documentation and closure

- [x] 4.1 Document installation, login, model/role assignment, additional provider extension, quota behavior, updates and rollback; check local links and official/source attribution.
- [x] 4.2 Record the user's accepted SuperGrok Heavy decision and redacted scenario evidence, obtain focused independent review of configuration/credential/recovery risks, resolve material findings and run OpenSpec strict validation before reporting completion.
