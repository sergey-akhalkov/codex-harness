## 1. Native RTK execution

- [x] 1.1 Add the pinned RTK dependency and reproducible native adapter build; verify checksum, version, build identity and idempotent provisioning in an owned root.
- [x] 1.2 Implement conservative hook classification and single-execution output filtering with raw recovery; verify quoting, Unicode, cwd/environment, nonzero exit, unsupported/machine syntax, bypass, filter failure and output bounds through the real runner.

## 2. Global integration

- [x] 2.1 Integrate the selected RTK exception and Code Mode with the linked installation lifecycle; verify install/update/repair/relocation/disconnect, unrelated-file preservation, core-only hooks-off behavior and continued diagnostic silence.
- [x] 2.2 Deliver actual native task-effort selection while preserving explicit overrides and model routes; verify effective lower/higher efforts and launcher argument forwarding, and document the installed active-turn boundary.

## 3. Workflow and semantic tools

- [x] 3.1 Deliver concise portable guidance for Code Mode, precise Serena/CBM retrieval, apply_patch, reuse and adaptive reasoning; verify global discovery and keep detailed guidance in one owning reference.
- [x] 3.2 Exercise fresh Serena/CBM consumers on a bounded external retrieval/edit/check scenario; verify project identity, index freshness/coverage, necessary results, native scoped editing and evidence invalidation, recording any interface improvement and measured compactness.

## 4. Acceptance and closure

- [x] 4.1 Activate the accepted global selection and establish exact native hook trust; verify a fresh outside consumer actually uses RTK through Code Mode, preserves a failed-call result, and leaves old diagnostic hooks inert.
- [x] 4.2 Measure representative raw/optimized command output and latency plus a matched native routine-task effort example with the same oracle; record source/runtime identity, failures and limits without extrapolating quota savings.
- [x] 4.3 Complete applicable regression checks and update installation, decisions, memory routing and evidence; verify local links and strict OpenSpec validation, then synchronize main specs and archive only when all implementation and acceptance requirements are fulfilled.
