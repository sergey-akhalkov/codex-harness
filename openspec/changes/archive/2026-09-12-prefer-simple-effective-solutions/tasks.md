## 1. Integrate the instruction behavior

- [x] 1.1 Revise the existing autonomy, discovery, simplicity, verification and reassessment sections of `global/principles-of-work.md` following design Decisions 1-3. Verify the scoped diff covers all delta requirements, consolidates repeated wording and preserves complete outcomes, required quality/performance checks, explicit constraints, authorization and legitimate data retention.
- [x] 1.2 Reconcile the relevant existing verification, token-workflow, memory, delegation and repository instruction owners listed in design Decision 1. Verify a scoped readback finds no conflicting obligation or unnecessary policy copy; leave compatible files unchanged and run existing behavioral checks for any task-specific mechanics actually edited. Preserve externally maintained OpenSpec workflows and unrelated working-tree changes.
- [x] 1.3 Record the confirmed decision concisely in `docs/project-decisions.md` and update the existing operation guidance in `docs/global-instructions.md`. Verify references lead to the canonical rule, loading and rollback instructions remain accurate, and neither file claims unperformed behavioral acceptance or contains private consumer details.

## 2. Exercise global delivery and engineering behavior

- [x] 2.1 Check fresh global loading outside this checkout, including a project with local instructions, using the existing model-free prompt-input route after inspecting installed help. Verify the canonical source and local constraints are present. Confirm a useful bounded child task in the acceptance work receives the guidance from a fresh parent; distinguish loading from its actual task result.
- [x] 2.2 Exercise the proof-request case from design Decision 4 in an owned checkout of a pre-existing external repository using its actual native verification path. Verify a representative success and meaningful failure with intact assertions, inspect real command results and resulting files, confirm no unnecessary proof store is introduced, and keep the case incomplete if only a synthetic substitute or prose answer is available.
- [x] 2.3 Exercise the remaining bounded cases in design Decision 4: routine correction, justified complexity with necessary retained data, reassessment with a binding-constraint variant, and an unobserved success claim. Verify actual decisions and applicable tool results preserve requirements, handle authority correctly and add no ritual or unsupported success claim; identify the limits of synthetic decision cases.

## 3. Close on actual acceptance

- [x] 3.1 Correct any observed instruction or behavioral failure within the accepted scope and repeat only affected checks. Verify all required cases pass, or preserve the exact unfinished task and blocker; successful loading alone cannot close behavior tasks, and acceptance cannot be narrowed to obtain a pass.
- [x] 3.2 Update the existing global-instruction guide with concise observed outcomes, operating conditions and remaining evidence limits. Verify useful lessons have one owning record, private execution details stay local, failed attempts are represented honestly where they affect conclusions, and no universal speed or cost claim exceeds the observations.
- [x] 3.3 Run strict validation of this change and all OpenSpec artifacts, applicable source-hygiene and local-link checks, and a scoped diff review. Verify the final instructions and delta agree, external workflows and unrelated accepted work are preserved, and checkboxes close only after their implementation and acceptance; leave the delta ready for the normal OpenSpec synchronization/completion workflow.
