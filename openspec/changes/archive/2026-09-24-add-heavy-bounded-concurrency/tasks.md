## 1. Core admission and aggregate envelope

- [x] 1.1 Replace single-lease heavy admission with a bounded slot set in `crates/harness-core/src/resource_admission.rs`: a shared lock on the legacy `heavy-command.lock` held for the admission lifetime, plus exclusive per-slot lock files acquired by try-any under the existing bounded poll and queue wait. Slot count 1 keeps the exclusive legacy lock and takes no slot file. Slot files alone do not admit. Other resources stay exclusive. Verify in the module's tests that slots within the bound are held concurrently, a caller beyond the bound waits and then fails at queue-wait expiry without disturbing holders, dropped or cancelled leases release exactly their slot and the shared legacy lock, an exclusive first-byte lock on `heavy-command.lock` blocks admission while slot files are free, and a held new admission blocks that same exclusive lock.
- [x] 1.2 Add the account aggregate Job owner to `crates/harness-core/src/process.rs`: named per account, created or opened by each caller, `JOB_OBJECT_LIMIT_JOB_MEMORY` only (`JobMemoryLimit`; no `JOB_OBJECT_LIMIT_PROCESS_MEMORY`, no CPU rate, no kill-on-close), handle-held lifetime, membership and limit snapshot. Nest it between the unchanged shared CPU job and the per-tree containment job. Verify with owned native fixtures that two independent processes join one kernel object, readback reports `JOB_OBJECT_LIMIT_JOB_MEMORY`, the configured `JobMemoryLimit`, an unset per-process memory limit and zero CPU rate, the object closes with the last handle, and name or limit conflicts fail closed with a clear error.
- [x] 1.3 Compose `crates/harness-core/src/heavy_command.rs` with slots and the aggregate envelope: policy parsing and validation for `max_concurrent_trees` (positive; 1 restores serialized admission) and `aggregate_memory_limit_bytes` (missing means the effective per-tree limit; an explicit value below that limit is refused), admission order slot -> shared CPU budget -> aggregate Job -> payload, per-tree containment Job kept inner, and nested heavy admission that inherits the outer admission instead of taking a second slot. Verify direct and nested runs, aggregate and per-tree readback in run diagnostics, invalid policy values, unchanged exit codes, and the documented no-deadlock admission order.

## 2. CLI, policy and status surfaces

- [x] 2.1 Update `crates/codex-harness/src/heavy_command_cli.rs` and its tests: `heavy budget` text and JSON report slot count, aggregate memory, per-tree memory and each field's source; the queue diagnostic prints one bounded line with busy and total slots plus available holder descriptions; `--account` isolation covers both slots and the envelope; reads never rewrite legacy policy bytes; installed `heavy --help` and `heavy budget --help` match the implementation.
- [x] 2.2 Extend every existing manager or check surface that already reports heavy budget fields with the new effective values, keeping native stdout protocols intact and adding no new policy mirror. Verify through the existing check-surface tests that status is query-only, reports degraded or unknown admission honestly, and does not start work or mutate policy.

## 3. Documentation

- [x] 3.1 Reconcile `docs/rust-native.md` and `docs/installation.md` with bounded heavy concurrency: slot policy and defaults, the `JOB_OBJECT_LIMIT_JOB_MEMORY` envelope, the legacy lock gate, queue diagnostics, serialized rollback via slot count 1, and the unchanged shared CPU ceiling. Record the design decision in `docs/project-decisions.md`. Verify examples against installed help using only synthetic identities and paths.

## 4. Acceptance and gates

- [x] 4.1 Run the installed acceptance from outside this checkout: two concurrent representative Rust build or test trees in two synthetic checkouts under one account, a third caller waiting with the slot diagnostic, cancellation of one admitted tree releasing its slot and cleaning up only its tree, a nested heavy call inside an admitted tree, a legacy policy file left byte-identical, slot count 1 restoring serialization, and a legacy exclusive lock on `heavy-command.lock` blocking a current caller while slot files are free. Declare sampling bounds first and retain kernel membership, `JOB_OBJECT_LIMIT_JOB_MEMORY` readback, aggregate memory and aggregate CPU evidence through the existing local verification lifecycle before claiming enforcement.
- [x] 4.2 Complete the applicable locked Rust formatting, check, clippy and workspace tests under the existing capped native verification route, then run `openspec validate add-heavy-bounded-concurrency --strict --no-interactive`, `openspec validate --all --strict --no-interactive`, scoped source and publication checks, and `git diff --check`. Close implementation tasks only on their actual evidence.
