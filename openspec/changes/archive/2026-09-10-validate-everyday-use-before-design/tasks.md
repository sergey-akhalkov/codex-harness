## 1. Owned instruction update

The optional design artifact is omitted because this instruction-only change introduces no technical architecture or runtime changes.

- [x] 1.1 Update the canonical global principles and owning decision record with everyday-use discovery, research before design and safe reuse while preserving the OpenSpec ownership boundary; review against the delta scenarios, including routine work, confirmed decisions, dependency provenance and hostile external instructions.
- [x] 1.2 Sync the project-owned global-working-principles specification while preserving unrelated existing requirements; validate the change and specification with OpenSpec strict checks.

## 2. Delivery and hygiene

- [x] 2.1 Run the native source/link check and scoped whitespace check; compare hashes to confirm external OpenSpec skills and workflow configuration remain unchanged.
- [x] 2.2 Verify that the supported global instruction link targets the canonical file and a fresh model-free prompt inspection outside the checkout contains the complete updated text exactly once; report that this proves loading, not model compliance or real-user validation.
