## 1. Instructions and retained knowledge

- [x] 1.1 Update portable principles, repository guidance and project-memory skill/templates for publication boundaries and purposeful retention; validate the skill and inspect global source links.
- [x] 1.2 Consolidate maintained docs and decisions, remove obsolete inventories/research/closed reports after preserving necessary facts, and retarget references; verify local links and preserve all other active task states.
- [x] 1.3 Sanitize retained OpenSpec and active migration context, preserving real-consumer acceptance through local inputs; validate all affected specs and compare unfinished task counts.

## 2. Source and output hygiene

- [x] 2.1 Remove private consumer exceptions and queries from report code and test fixtures; verify actual Rust and transitional report entry points with synthetic private labels and unchanged accounting.
- [x] 2.2 Remove tracked runtime caches and expand ignore rules; verify Git-listed source contains no tracked generated caches or locally supplied private identifiers.
- [x] 2.3 Provide the small Rust source/link hygiene check with non-disclosing diagnostics; verify synthetic pass/failure cases and run it on the resulting repository.

## 3. Portable configuration and activation

- [x] 3.1 Implement live shared defaults with machine-local native persistence, preserving explicit invocation overrides; run applicable launcher/installer checks and actual isolated native trust/model persistence checks.
- [x] 3.2 Preserve existing local settings, clean the shared config and activate the corrected boundary through the supported lifecycle; verify installed instructions, memory skill and ordinary CLI behavior outside the checkout without disrupting the controlling session.

## 4. Integrated acceptance

- [x] 4.1 Run strict OpenSpec validation, final source/privacy/link checks and all affected runtime checks; record concise outcomes and limitations in this change, verify pre-existing work was preserved and close only supported tasks.
