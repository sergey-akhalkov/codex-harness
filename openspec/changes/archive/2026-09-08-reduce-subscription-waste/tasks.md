## 1. Immediate suspension requested before planning

- [x] 1.1 Back up the host's current hook/configuration state, empty the linked global hook definitions and disable the native hooks feature in base/profile configuration; verify parsed settings and native feature discovery outside this checkout.
- [x] 1.2 Suppress cached command and broker hook handlers using the host-local suspension marker and gracefully replace the broker; verify all three command events exit silently and cached MCP PostToolUse/Stop/SubagentStop requests return no diagnostic context.
- [x] 1.3 Verify a fresh native consumer outside this repository lists no hooks, and record source identities, recovery information and evidence limits in `docs/evidence/hooks-suspension.md`.

## 2. Attribute existing usage without generating more work

- [x] 2.1 Reuse the existing usage/outcome reporters to produce robust deduplicated accounting; verify fixtures for repeated response IDs, cumulative counters, cached input, reasoning included in output, missing child records and partial/concurrent periods.
- [x] 2.2 Produce a compact report from existing local rollouts covering model/effort, parent/children, project, automatic context and actual continuations; verify traceable private inputs, no raw transcripts/secrets in Git, and no invented weekly-quota conversion.
- [x] 2.3 Record the candidate comparisons and reuse applicable `accelerate-verified-delivery` evidence; verify fixed acceptance oracles, source/model identities, meaningful timing differences and the design's finite stopping rules before any additional model-backed run.

## 3. Minimize automated input at its producer

- [x] 3.1 Inventory kit-owned automated input paths from manifests, registrations and entry points; verify coverage of hooks, LSP/MCP adapters, lifecycle/status/fallback messages, tool descriptions, wrappers and delegated handoffs using one ordinary/no-op and one failure/change sample per interface.
- [x] 3.2 Batch related producer changes to eliminate routine dumps, duplicate policy and unchanged notifications, retaining actionable failures and explicit detail retrieval; verify protocol-valid output, quiet no-op cases, semantic deduplication, necessary uncertainty and interface-specific size limits, with before/after context measurements.

## 4. Select capabilities by accepted-result benefit

- [x] 4.1 Qualify the installed Serena file-diagnostics API and editing surface as a shared-provider candidate using a compatible client and owned fixtures; verify real language/edit/creation support, project/client isolation, filesystem synchronization scope, current error/clearance versus missing or cached results, output-limit behavior and cold/warm process reuse. Record any hard contract failure as evidence to keep automatic use off, without a broad Serena rewrite.
- [x] 4.2 Select the simplest useful workflow, preferring existing Serena edits/explicit diagnostics plus project-native checks, and compare ready-made inline diagnostics only if useful; verify fixed task completion/defect/time oracles, total parent/child usage and native/recovery fallbacks within the bounded budget. Keep development without hooks as the durable default. Any hook exception requires a concrete unmet need, native-alternative assessment, bounded deterministic behavior, silent success and accepted total cost; examples alone create no implementation tasks. Record an important unmet need before custom diagnostics; no tool-use percentage target or speculative prototypes.
- [x] 4.3 Apply the selected capability set to reusable registrations, provisioning and lifecycle sources; verify rejected hooks/backends stay disabled, accepted Serena reuse removes only proven overlap, shared packages and unrelated MCPs remain intact, and the language matrix honestly distinguishes explicit, automatic and retired support. Retiring all LSP is a valid completed branch.

## 5. Enforce strict behavior for any retained automatic LSP

These tasks apply only to scopes accepted by 4.2. If none qualify, close each with the recorded selection and executable proof that the corresponding automatic path is absent; do not implement unused machinery.

- [x] 5.1 Gate analysis on proven supported-file creation or changed content across native patch, shell and MCP edits, with bounded observations and no baseline scan; verify untracked/pre-dirty files, byte-identical writes, failed writers, rename destinations, unknown history and concurrent unrelated changes.
- [x] 5.2 Restrict backend requests and delivery to eligible changed files, with necessary dependency reads only; verify configuration/cache invalidation, sibling Markdown links, unsupported/oversized files and deletion-only operations never fan out to unchanged cohorts or invent coverage.
- [x] 5.3 Remove completion-triggered analysis and pending reconciliation from retained automatic paths; verify zero analysis requests and zero context on reads, startup, unchanged commands, Stop/SubagentStop and historical pending work, including timeout/recovery cases.
- [x] 5.4 Deliver one current, deduplicated, minimal summary per mutation within the finite budget and 1000-character ceiling, preserving the original tool result; verify stale-reply rejection, overlapping edits, error/correction, timeout/unavailable output, full-detail retrieval and no feedback loops across command/MCP transports.
- [x] 5.5 Exercise every retained automatic capability against actual Codex patch, shell and MCP operations in owned consumers outside this repository; verify positive delivery for selected entry points, zero requests/output plus explicit-check coverage for non-selected routes, the negative trigger matrix, concurrent roots and original file/exit-status preservation, using existing checks where applicable.

## 6. Optimize model and context costs within existing policy

- [x] 6.1 Use the attribution report to assess Astra effort and context-loading/setup costs on representative accepted tasks; verify comparable outcomes before changing any default, preserve mandatory skill/tool-freshness semantics, and leave inconclusive settings unchanged.
- [x] 6.2 Consume the owning Grok-recovery change's verified result and inspect partial child work before repeating assignments; verify observed routing/failure evidence, Astra-only OpenAI assignments, preferred available Grok middle and no silent provider/billing/Fast-mode substitution.

## 7. Global acceptance and consistent contracts

- [x] 7.1 Preserve suspension, the durable hooks-disabled default and any specifically accepted selection through fresh install, update, repair, relocation, recovery, completion/archive and disconnect; verify lifecycle fixtures plus a real outside-repository consumer, cached-handler suppression and rollback to hooks-off without foreign-data loss or automatic restoration from backups.
- [x] 7.2 Reconcile conflicting active deltas and synchronize the accepted main specs and current documentation without rewriting historical evidence; verify no unconditional language, Stop/reconciliation or installer rule can silently restore rejected behavior, and check OpenSpec validation and local links.
- [x] 7.3 Publish the final selected configuration, accepted-result comparisons, context/usage observations and remaining attribution limits; verify all applicable tasks and consuming-project checks have evidence, with no claim that silence means clean diagnostics or that token counts equal weekly savings.

## Acceptance evidence

- 1: [Emergency suspension](../../../../docs/evidence/hooks-suspension.md), followed by durable source gates and native outside-consumer verification.
- 2, 6: [Usage accounting](../../../../docs/evidence/subscription-usage.md) and [candidate comparisons / routing limits](../../../../docs/evidence/subscription-efficiency.md#bounded-candidate-assessment). Native child identity and conflicting response copies are corrected; 34 accounting and 32 consumer checks pass. An independent reduction matches all five response fields on 227 frozen historical inputs; unreconciled totals remain unknown.
- 3: [Producer inventory and bounds](../../../../docs/evidence/subscription-efficiency.md#automated-input-inventory), including actual native catalogues and failure detail retrieval.
- 4: [Installed Serena qualification](../../../../docs/evidence/serena-efficiency.md) and [language selection](../../../../docs/code-tools.md#current-language-selection); native tiny edits remain preferable, no automatic scope or hook exception is accepted.
- 5.1–5.5: all take the explicitly permitted zero-retained-scopes branch. `tests/subscription-efficiency.py` proves command/MCP/broker no-analysis boundaries across the negative matrix and preserves explicit native checks and writer status. No unused mutation/reconciliation machinery was implemented.
- 7: [Lifecycle and global acceptance](../../../../docs/evidence/subscription-efficiency.md#verification-and-private-evidence); main spec sync preserves unaffected requirements, and both reconciled active changes pass strict validation.
