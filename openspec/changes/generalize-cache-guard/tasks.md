## 1. Qualify and implement native warning transport

- [ ] 1.1 Capture the installed Codex app-server warning contract in an owned temporary schema export and implement only the version-supported exact-thread `warning` notification model in `crates/codex-harness/src/executor_warning.rs`; refuse unsupported shapes rather than guessing. Verify serialization against the generated schema and a unit test that no client-to-server warning request is invented.
- [ ] 1.2 Implement the harness-owned frontend-facing loopback WebSocket relay for `codex resume --remote` in `crates/codex-harness/src/executor_warning.rs`, preserving capability-token authentication and bidirectional app-server traffic while allowing the host to queue one deduplicated degraded diagnostic. Verify handshake rejection, byte-preserving forwarding, warning insertion order, close behavior, and no credential/log leakage with deterministic Rust tests.
- [ ] 1.3 Integrate the relay into native frontend attachment in `crates/codex-harness/src/executor_cli.rs` without changing the recorded app-server endpoint used by `executor message` and `executor stop`. Verify concurrent control requests, frontend attach/resume, relay shutdown, and survival of unrelated app-server traffic through the existing executor-control fixtures plus the new relay tests.

## 2. Generalize exact-session cache monitoring

- [ ] 2.1 Remove the DeepSeek-only monitor filter in `crates/codex-harness/src/executor_cache.rs`, retain the resolved provider/model identity, and keep numeric counter validation, duplicate ordering, historical warmup, and exact-session isolation unchanged. Verify OpenAI/Z.AI/xAI-style identities, never-warmed zero-cache counters, malformed counters, cold starts, and historical resume with unit tests in `executor_cache.rs`.
- [ ] 2.2 Make sustained-loss evidence and stop wording provider-neutral in `crates/codex-harness/src/executor_cache.rs` while preserving thresholds, consecutive counting, owned-tree termination, partial-stop reporting, and recovery evidence. Extend `crates/codex-harness/tests/fixtures/cache_usage.rs` and the control/observation cache-loss tests so one non-DeepSeek synthetic run stops and one never-warmed run continues.
- [ ] 2.3 Extend receipt state in `crates/codex-harness/src/executor_cache.rs` and its consumers with bounded runtime-support and diagnostic-delivery fields for proven, unproven, invalid, unavailable, native-sent, undelivered, and no-native-consumer states. Verify all fields through existing receipt/watch consumers and tests without exposing credentials or private paths.

## 3. Route diagnostics without corrupting the native input surface

- [ ] 3.1 Replace cache-guard terminal `println!` diagnostics in `crates/codex-harness/src/executor_cache.rs`, `executor_cli.rs`, and `executor_observation.rs` with classified receipt/log status and the native warning queue. Verify routine enabled/available/warming/armed transitions produce no native warning or direct terminal line, while invalid/unavailable/truncated/identity diagnostics are queued once.
- [ ] 3.2 Deliver queued diagnostics through `executor_warning.rs` only after the native frontend initializes the exact thread, and record send/undelivered/no-consumer state without claiming that the user opened the viewer. Verify send failure does not terminate healthy work, the compatibility renderer records no native consumer, and no diagnostic is written to the inherited terminal while a native frontend owns it.
- [ ] 3.3 Preserve cache-loss terminal behavior across control-backed spawn and observed exact-session resume in `crates/codex-harness/src/executor_cli.rs` and `executor_observation.rs`. Verify stop-first cleanup, non-success receipt/watch result, retained work/session/slot, bounded handoff, and fresh-session restart remain covered, including with a non-DeepSeek identity.

## 4. Verify, document, and deliver

- [ ] 4.1 Update `docs/agent-delegation.md`, `docs/rust-native.md`, and affected CLI help/receipt documentation for provider-neutral runtime-proven protection and native warning presentation; state the send-versus-seen limit and compatibility fallback. Verify local links, installed help, factual claims, source hygiene, and publication boundaries.
- [ ] 4.2 Run targeted locked Rust tests for `executor_cache`, executor control/observation, frontend warning transport, and receipt/watch consumers; then run formatting, explicit compiler/clippy diagnostics, executable-ownership accounting, and the applicable native checks from `docs/rust-native.md`. Record exact commands and fresh results, and fix regressions before delivery.
- [ ] 4.3 Exercise installed behavior outside this checkout through the immutable lifecycle: one non-DeepSeek synthetic provider with runtime-proven warmup/loss/stop/restart, one degraded diagnostic visible in the native warning viewer, and one no-native-consumer compatibility run. Preserve unrelated settings and active sessions, retain build identity, and verify rollback to the prior immutable build.
- [ ] 4.4 Reconcile integration with active `simplify-native-harness`, validate `openspec validate --all --strict --no-interactive` and `git diff --check`, and close tasks only on their recorded actual checks. Confirm no externally maintained OpenSpec or Codex sources were modified and no requirement from the overlapping change was weakened.
