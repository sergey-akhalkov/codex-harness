## 1. Verified native control foundation

- [x] 1.1 Establish the installed Codex control contract in owned isolated state: verify exact CLI/schema identity, two-client event delivery, native TUI visibility, task-scoped correlation, child survival and reconnect; retain a concise contract receipt and original failures before dependent implementation.
- [x] 1.2 Implement the smallest Rust control connection using the verified native path and existing process/lifecycle conventions; verify initialization, exact model binding, one tool-using assignment and final-result delivery through the ordinary installed entry point without manual secondary startup.
- [x] 1.3 Extend that path to a deterministic GPT quota refusal before a fallback instruction; verify a fresh Z.AI lead obtains the saved task and performs the next accepted step without another GPT response. Keep the implementation incomplete if only policy guidance works.
- [ ] 1.4 Establish automatic simultaneous visible windows/panes for every active leader, executor and model-backed helper before live dispatch; verify streaming conversation/tool activity, profile/model/effort/status, explicit lead handoff, retained history and no hidden new requests when a view fails or closes. A list with chat switching and the previous single-TUI proof do not satisfy this check.

## 2. Configurable orchestration roles

- [ ] 2.1 Define the kit-owned orchestration configuration schema (lead profile, executor profiles, maximum concurrent executors) and validate it through the existing installation check; verify missing profiles and invalid limits produce explicit errors without substitution.
- [ ] 2.2 Dispatch a configured executor through its exact profile from a fresh external session; verify effective model/effort identities, native overrides and an actual subscribed tool response.
- [x] 2.3 Retire redundant supplied core agent presets and their obsolete policy references; preserve skills and user-owned agents, document direct model/effort migration, and verify source checks plus fresh native discovery without those presets. Finish subscription-owned preset/link retirement through task 7.1.
- [ ] 2.4 Generalize lead succession and executor selection to the configuration, replacing the hard-coded successor binding while preserving the verified handoff evidence.
- [ ] 2.5 Document the orchestration configuration in the owning guide with synthetic examples.

## 3. Board-based asynchronous coordination

- [ ] 3.1 Assess the `beads` (`bd`) dependency proportionally before execution: authentic identity, license, maintenance, install effects and vulnerabilities; record the decision and supported version path.
- [ ] 3.2 Verify the non-interactive `bd` command contract on Windows in an owned isolated project: stage/epic, spec/feature, feedback task and status/report operations.
- [ ] 3.3 Connect board availability and agent workflow guidance through the kit lifecycle; verify fresh external sessions discover them and that unrelated configuration and credentials are preserved.
- [ ] 3.4 Exercise the board workflow end to end in an isolated project: executor feedback tasks reach the lead, the lead processes and resolves them, and improvement items become tasks or OpenSpec changes; keep synthetic public examples only.
- [ ] 3.5 Create the `team-lead` skill as owner of the lead workflow instructions (explicit activation, role configuration discovery, board setup, executor briefs, steering, acceptance, merge, explicit stop); verify activation changes only the activated session, clear uninvoked requests can select it by description, and ordinary sessions spawn nothing.

## 4. Isolated visible executors and steering

- [ ] 4.1 Implement controller-owned worktree lifecycle: create per assignment from the task base revision, record the mapping, preserve through interruption, retire after merge or explicit discard; verify executors never write to the shared checkout.
- [ ] 4.2 Integrate profile-backed executor dispatch with its own window and worktree before the first model request; verify two executors run simultaneously in separate worktrees and windows with distinct live identities.
- [ ] 4.3 Deliver lead steering through the verified session channel into the executor's visible conversation; verify no hidden model calls, no status polling and continuation in the same session and worktree.
- [ ] 4.4 Verify view-loss behavior: suspension of new dispatch, reconciliation without replay, explicit reporting and restoration before continuation.

## 5. Lead acceptance and merge

- [ ] 5.1 Implement lead review and merge: acceptance against requirements and applicable checks, merge by the lead, worktree retirement and board/task-state records.
- [ ] 5.2 Return concrete defects with acceptance conditions to the original executor; verify corrected work preserves partial results and the executor's ownership.

## 6. Durable state, failures and succession

- [ ] 6.1 Add host-private task records and atomic dispatch/ownership transitions; verify interrupted writes preserve recoverable state, canonical workspace/worktree identity, accepted requirements and read-only/planning authorization.
- [ ] 6.2 Correlate leaders, assignments, attempts and resource ownership with native sessions; verify one active leader, the configured concurrency bound, and preservation of an existing executor assignment during leadership transfer.
- [ ] 6.3 Reconcile surviving workers and delayed results before replacement; verify quota failure after a partial edit, stale completion and uncertain external effects preserve work without conflicting writers or blind effect replay.
- [ ] 6.4 Classify quota exhaustion, temporary throttling, authentication/model rejection, transport failure and incomplete output from retained provider evidence; verify representative error fixtures preserve original cause, scope and uncertainty.
- [ ] 6.5 Implement capability-aware reassignment and all-capacity suspension within the configured pool; verify continuation without purchases or infinite model retries.
- [ ] 6.6 Implement restart, resume and explicit Stop/Disconnect semantics; verify an unexpectedly interrupted active task resumes only after reconciliation, whereas a stopped task stays stopped after restart.

## 7. Guidance and global lifecycle

- [ ] 7.1 Update owning global policy and role guidance for the team-lead pattern, board coordination, patient ownership and result reconciliation; add the single policy pointer to the `team-lead` skill without duplicating its workflow; reconcile overlapping decomposition guidance without closing its tasks, and retire subscription-owned preset links and checks through the lifecycle owners.
- [ ] 7.2 Connect the controller, views, role configuration and board availability through native install/update/check/recover/disconnect; verify isolated preview is read-only, repeat installation is idempotent, interrupted activation is recoverable, and unrelated settings/authentication/processes are preserved.
- [ ] 7.3 Verify fresh ordinary Windows Codex sessions outside the checkout load the updated policy, configuration, tools and board workflow; exercise simultaneous windows, lead transfer, view loss/reconnection, user stop, explicit profile overrides and final-result visibility through the real entry point.
- [ ] 7.4 Verify disconnect and rollback preserve private task checkpoints and worktrees, restore native GPT operation, leave stopped tasks stopped, and do not stop unrelated runtimes.

## 8. End-to-end acceptance

- [ ] 8.1 Run the owned failure matrix through the integrated entry point: lead refusal before succession, worker refusal after partial work, delayed completion, throttling, auth/model/transport errors, board unavailability, all-capacity exhaustion, restart and explicit stop; verify unchanged spec outcomes without exhausting live accounts.
- [ ] 8.2 Exercise the installed workflow on a locally selected real external development task with actual subscribed tool work through configured executor profiles; verify its results are consumed and applicable consuming-project acceptance passes, with private evidence outside shared sources.
- [ ] 8.3 Run applicable Rust formatting, static analysis, native and transitional launcher/delegation/subscription/lifecycle checks for the changed implementation; verify exact build identity and close only tasks supported by actual results.
