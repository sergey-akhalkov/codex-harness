## 1. Prove native control and the first useful handoff

- [x] 1.1 Establish the installed Codex control contract in owned isolated state: verify exact CLI/schema identity, two-client event delivery, native TUI visibility, task-scoped correlation, child survival and reconnect; retain a concise contract receipt and original failures before dependent implementation.
- [x] 1.2 Implement the smallest Rust control connection using the verified native path and existing process/lifecycle conventions; verify initialization, exact model binding, one tool-using assignment and final-result delivery through the ordinary installed entry point without manual secondary startup.
- [ ] 1.3 Extend that path to a deterministic GPT quota refusal before a fallback instruction; verify a fresh Z.AI lead obtains the saved task and performs the next accepted step without another GPT response. Keep the implementation incomplete if only policy guidance works.
- [ ] 1.4 Establish automatic simultaneous visible windows/panes for every active leader, executor and model-backed helper before live dispatch; verify streaming conversation/tool activity, model/effort/status, explicit lead handoff, retained history and no hidden new requests when a view fails or closes. A list with chat switching and the previous single-TUI proof do not satisfy this check.

## 2. Preserve task state and ownership

- [ ] 2.1 Add host-private task records and atomic dispatch/ownership transitions; verify interrupted writes preserve recoverable state, canonical workspace identity, accepted requirements and read-only/planning authorization.
- [ ] 2.2 Correlate leaders, assignments, attempts and resource ownership with native sessions; verify one active leader, the shared two-executor bound, and preservation of an existing Z.AI assignment during leadership transfer.
- [ ] 2.3 Reconcile surviving workers and delayed results before replacement; verify quota failure after a partial edit, stale completion and uncertain external effects preserve work without conflicting writers or blind effect replay.
- [ ] 2.4 Implement restart, resume and explicit Stop/Disconnect semantics; verify an unexpectedly interrupted active task resumes only after reconciliation, whereas a stopped task stays stopped after quota reset and runtime restart.

## 3. Route work and recover unavailable capacity

- [ ] 3.1 Deliver direct model/effort selection for Z.AI senior execution and temporary leadership, Grok execution and Astra reserves without requiring named presets; verify supported combinations, effective identities, native overrides and actual subscribed model/tool responses from a fresh external session.
- [ ] 3.2 Classify quota exhaustion, temporary throttling, authentication/model rejection, transport failure and incomplete output from retained provider evidence; verify representative error fixtures preserve original cause, scope and uncertainty instead of treating every rate-limit error as weekly exhaustion.
- [ ] 3.3 Persist account-scoped availability and bounded retry/reset eligibility; verify several tasks sharing an account avoid repeated unavailable dispatch and reset-time bursts, while common proxy failure retains its separate bounded recovery policy.
- [ ] 3.4 Implement capability-aware reassignment and all-capacity suspension; verify Z.AI-to-Grok text fallback, demanding-work escalation, visual-capability preservation, native GPT independence and resumption without purchases or infinite model retries.
- [ ] 3.5 Complete temporary Z.AI leadership, preservation of capable remaining executors and GPT return at a safe boundary; verify refusal while both workers run, unavailable GPT plus Z.AI, and restored GPT without interrupting or duplicating healthy work.
- [x] 3.6 Retire redundant supplied core agent presets and their obsolete policy references; preserve skills and user-owned agents, document direct model/effort migration, and verify source checks plus fresh native discovery without those presets. Finish subscription-owned preset/link retirement through task 5.1.

## 4. Make delegation patient and economical

- [ ] 4.1 Update owning global policy and role guidance for complete workstream assignments, correction by the original executor and selective lead review; verify substantial text/code and visual tasks select the intended subscriptions and trivial work remains directly executable.
- [ ] 4.2 Replace incomplete-output parent-takeover guidance with result reconciliation and cause-based recovery; verify an active slow worker is not raced or pressured, an empty completion is not treated as exhaustion, and only concrete new facts or failures cause steering/reassignment.
- [ ] 4.3 Connect reliable account observations where supported, preserving unknown values elsewhere; verify reset-window scope, stale/missing telemetry and cross-project account use without speculative model requests or false quota percentages.
- [ ] 4.4 Apply quota-aware pacing to new assignments and supported reasoning choices at task/turn boundaries; verify no active worker is preempted for pacing and no ongoing task is restarted solely to lower effort.
- [ ] 4.5 Reuse existing usage reporting for leader, workers, retries, coordination and rework; verify response deduplication, GPT descendants on the parent's subscription, bounded silent no-op handling and accessible private detail evidence.
- [ ] 4.6 Reconcile the overlapping `adapt-workflow-through-decomposition` requirement and owning guidance without closing its tasks; verify all existing resource ownership, validity, restoration and result-consumption scenarios remain represented after integration.

## 5. Deliver through the global lifecycle

- [ ] 5.1 Connect the controller, visible conversation views and direct model selection through native install/update/check/recover/disconnect owners; retire obsolete managed preset links and checks, and verify isolated preview is read-only, repeat installation is idempotent, interrupted activation is recoverable, and unrelated settings/authentication/processes are preserved.
- [ ] 5.2 Verify fresh ordinary Windows Codex sessions outside the checkout load the updated policy, backend settings, tools and exact bindings; exercise simultaneous active chat windows/panes, lead transfer, view loss/reconnection, user stop, explicit model/profile overrides and final-result visibility through the real entry point.
- [ ] 5.3 Verify disconnect and rollback preserve private task checkpoints, restore native GPT operation, leave stopped tasks stopped, and do not stop the shared proxy or controller serving unrelated active tasks.
- [ ] 5.4 Update the owning delegation, subscription, installation, token-workflow and decision records with actual supported operation, compatibility limits and recovery commands; run native source hygiene/local-link checks and keep private inputs/evidence outside shared source.

## 6. Verify the full behavior and demonstrated benefit

- [ ] 6.1 Define a bounded matched comparison before model-backed execution: text implementation, visual verification and parallel integration; record fixed inputs, acceptance checks, direct-Astra baseline, timing tolerance, usage attribution, attempt budget and stopping rule, reusing applicable existing evidence.
- [ ] 6.2 Run the owned failure matrix through the integrated entry point: leader refusal before fallback, worker refusal after partial work, delayed completion, throttling, auth/model/transport errors, unavailable telemetry, modality mismatch, all-capacity exhaustion, reset, restart and explicit stop; verify the unchanged spec outcomes without exhausting or disrupting live accounts.
- [ ] 6.3 Exercise the installed workflow on a locally selected real external development task with actual subscribed Z.AI/Grok tool work; verify its results are consumed, meaningful failure paths are checked, and all required consuming-project acceptance passes.
- [ ] 6.4 Complete the declared direct-versus-delegated comparison including waiting, review and corrections; verify lower attributable GPT use, unchanged correctness and no material delivery-time regression beyond declared measurement tolerance. Correct failures and leave inconclusive benefit work open without claiming weekly-quota savings.
- [ ] 6.5 Run applicable Rust formatting, static analysis, native and transitional launcher/delegation/subscription/lifecycle checks for the changed implementation; verify exact build identity, report substitute-environment limits and close only tasks supported by actual results.
