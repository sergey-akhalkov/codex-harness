## 1. Verified native control foundation

- [x] 1.1 Establish the installed Codex control contract in owned isolated state: verify exact CLI/schema identity, two-client event delivery, native TUI visibility, task-scoped correlation, child survival and reconnect; retain a concise contract receipt and original failures before dependent implementation.
- [x] 1.2 Implement the smallest Rust control connection using the verified native path and existing process/lifecycle conventions; verify initialization, exact model binding, one tool-using assignment and final-result delivery through the ordinary installed entry point without manual secondary startup.
- [x] 1.3 Extend that path to a deterministic GPT quota refusal before a fallback instruction; verify a fresh Z.AI lead obtains the saved task and performs the next accepted step without another GPT response. Keep the implementation incomplete if only policy guidance works.
- [x] 1.4 Establish automatic simultaneous visible windows/panes for every active leader, executor and model-backed helper before live dispatch; verify streaming conversation/tool activity, profile/model/effort/status, explicit lead handoff, retained history and no hidden new requests when a view fails or closes. A list with chat switching and the previous single-TUI proof do not satisfy this check.

## 2. Configurable orchestration roles

- [x] 2.1 Define the kit-owned orchestration configuration schema (lead profile, executor profiles, maximum concurrent executors) and validate it through the existing installation check; verify missing profiles and invalid limits produce explicit errors without substitution.
- [x] 2.2 Dispatch a configured executor through its exact profile from a fresh external session; verify effective model/effort identities, native overrides and an actual subscribed tool response.
- [x] 2.3 Retire redundant supplied core agent presets and their obsolete policy references; preserve skills and user-owned agents, document direct model/effort migration, and verify source checks plus fresh native discovery without those presets. Finish subscription-owned preset/link retirement through task 7.1.
- [x] 2.4 Generalize lead succession and executor selection to the configuration, replacing the hard-coded successor binding while preserving the verified handoff evidence.
- [x] 2.5 Document the orchestration configuration in the owning guide with synthetic examples.

## 3. Board-based asynchronous coordination

- [x] 3.1 Assess the `beads` (`bd`) dependency proportionally before execution: authentic identity, license, maintenance, install effects and vulnerabilities; record the decision and supported version path.
- [x] 3.2 Verify the non-interactive `bd` command contract on Windows in an owned isolated project: stage/epic, spec/feature, feedback task and status/report operations.
- [x] 3.3 Connect board availability and agent workflow guidance through the kit lifecycle; verify fresh external sessions discover them and that unrelated configuration and credentials are preserved.
- [x] 3.4 Exercise the board workflow end to end in an isolated project: executor feedback tasks reach the lead, the lead processes and resolves them, and improvement items become tasks or OpenSpec changes; keep synthetic public examples only.
- [x] 3.5 Create the `team-lead` skill as owner of the lead workflow instructions (explicit activation, role configuration discovery, board setup, executor briefs, steering, acceptance, merge, explicit stop); verify activation changes only the activated session, clear uninvoked requests can select it by description, and ordinary sessions spawn nothing.

## 4. Isolated visible executors and steering

- [x] 4.1 Verify installed CLI managed worktrees on Windows (allocation as on 0.154.0; owner identity and confirmed deletion as on 0.155.0) and use them as the executor checkout: enable experimental `worktrees`, allocate a native managed worktree from the task base revision, bind it before the first model request, record the native owner mapping, preserve it through interruption, and retire it after merge or explicit discard. Use native confirmed deletion only for a clean managed worktree of the current repository that is not the current checkout and has no local, untracked or ignored changes; otherwise preserve the checkout and report the limit. Do not treat agents-overview hide/archive/task deletion as retirement, do not substitute an ordinary Git worktree, and do not invent a second harness tree. Because the verified control TUI uses `--remote`, do not pass `--worktree` on that view; attach the visible window to the managed cwd. Add harness mapping only where native is insufficient (remote TUI, ephemeral spawn_agent helpers, missing feature, ineligible native delete). Verify executors never write to the shared checkout and that unsupported installations report an explicit limitation instead of using the shared tree.
- [x] 4.2 Integrate profile-backed executor dispatch with its own window and worktree before the first model request; verify two executors run simultaneously in separate worktrees and windows with distinct live identities.
- [x] 4.3 Deliver lead steering through the verified session channel into the executor's visible conversation; verify no hidden model calls, no status polling and continuation in the same session and worktree.
- [x] 4.4 Verify view-loss behavior: suspension of new dispatch, reconciliation without replay, explicit reporting and restoration before continuation.

## 5. Lead acceptance and merge

- [x] 5.1 Implement lead review and merge: acceptance against requirements and applicable checks, merge by the lead, worktree retirement through native confirmed deletion when eligible otherwise preserved with an explicit limitation, and board/task-state records.
- [x] 5.2 Return concrete defects with acceptance conditions to the original executor; verify corrected work preserves partial results and the executor's ownership.

## 6. Durable state, failures and succession

- [x] 6.1 Add host-private task records and atomic dispatch/ownership transitions; verify interrupted writes preserve recoverable state, canonical workspace/worktree identity, accepted requirements and read-only/planning authorization.
- [x] 6.2 Correlate leaders, assignments, attempts and resource ownership with native sessions; verify one active leader, the configured concurrency bound, and preservation of an existing executor assignment during leadership transfer.
- [x] 6.3 Reconcile surviving workers and delayed results before replacement; verify quota failure after a partial edit, stale completion and uncertain external effects preserve work without conflicting writers or blind effect replay.
- [x] 6.4 Classify quota exhaustion, temporary throttling, authentication/model rejection, transport failure and incomplete output from retained provider evidence; verify representative error fixtures preserve original cause, scope and uncertainty.
- [x] 6.5 Implement capability-aware reassignment and all-capacity suspension within the configured pool; verify continuation without purchases or infinite model retries.
- [x] 6.6 Implement restart, resume and explicit Stop/Disconnect semantics; verify an unexpectedly interrupted active task resumes only after reconciliation, whereas a stopped task stays stopped after restart.

## 7. Guidance and global lifecycle

- [x] 7.1 Update owning global policy and role guidance for the team-lead pattern, board coordination, patient ownership and result reconciliation; add the single policy pointer to the `team-lead` skill without duplicating its workflow; reconcile overlapping decomposition guidance without closing its tasks, and retire subscription-owned preset links and checks through the lifecycle owners.
- [x] 7.2 Connect the controller, views, role configuration and board availability through native install/update/check/recover/disconnect; verify isolated preview is read-only, repeat installation is idempotent, interrupted activation is recoverable, and unrelated settings/authentication/processes are preserved.
- [x] 7.3 Verify fresh ordinary Windows Codex sessions outside the checkout load the updated policy, configuration, tools and board workflow; exercise simultaneous windows, lead transfer, view loss/reconnection, user stop, explicit profile overrides and final-result visibility through the real entry point.
- [x] 7.4 Verify disconnect and rollback preserve private task checkpoints and worktrees, restore native GPT operation, leave stopped tasks stopped, and do not stop unrelated runtimes.

## 8. End-to-end acceptance

- [x] 8.1 Run the owned failure matrix through the integrated entry point: lead refusal before succession, worker refusal after partial work, delayed completion, throttling, auth/model/transport errors, board unavailability, all-capacity exhaustion, restart and explicit stop; verify unchanged spec outcomes without exhausting live accounts.
- [ ] 8.2 Exercise the installed workflow on a locally selected real external development task with actual subscribed tool work through configured executor profiles; verify its results are consumed and applicable consuming-project acceptance passes, with private evidence outside shared sources.
- [x] 8.3 Run applicable Rust formatting, static analysis, native and transitional launcher/delegation/subscription/lifecycle checks for the changed implementation; verify exact build identity and close only tasks supported by actual results.
