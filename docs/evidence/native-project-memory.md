# Git project memory acceptance

2026-09-08–09. Owned evidence root:
`%LOCALAPPDATA%/codex-harness-evidence/memory-native-6ffd5f38ca764236bb27ece92c5eab3e/`.
These are controlled fixture decisions and recovery cases, not new harness-wide
user preferences. Both Codex runs used the installed source-linked skills from
outside this checkout, with separate new Codex homes and native memories disabled.

## Retrieval, verification and a useful update

The independent Rust `receipt-display` repository began at
`6171e2c669647dc137cf433f1b3e1c0a11889028`. Its instruction route linked a short
memory index to an accepted display decision, historical verification command,
and irrelevant packaging topic. A native baseline build succeeded; the current
CLI test intentionally exposed incorrect decimal rendering.

The first process exited naturally with code 0 but reported a blocked task:
Windows `CreateProcessAsUserW` failed with `-1073283067` in the probe's selected
`workspace-write` mode. It read no project or skill bodies and changed no files.
`first-run/` and `first-final.txt` retain that failure. A cancellation marker was
written after the process had already finished and did not cause its exit.

The accepted retry used the installed harness's ordinary `danger-full-access`
mode and the same owned task scope, 512 MiB Job and 300-second deadline. Explicit
route: `gpt-6-astra`, low effort, provider `openai`, existing ChatGPT through the
unchanged OpenCodex endpoint. Authentication/catalog files were linked, without
copying credentials. Native CLI 0.153.4 SHA-256:
`444A3F0008050605CAE73CD9B7A2DCAC61294062DFAAB56DD20430FD6498518B`.
No model, provider or billing substitution was made.

The agent read project-memory/project-verification, the fixture instruction/index,
and only the relevant owning records. Navigation MCPs were not configured in the
owned core-only home; the agent explicitly used scoped source reads. Visible
command events show no packaging-body retrieval. Its hash and the unrelated
sentinel's hash remained unchanged.

The agent corrected CLI rendering, exercised the actual executable, replaced the
obsolete `legacy_cli` command evidence, retained its historical status, and
recorded one new verified fact: 1,008 ASCII `A` bytes produce `checksum=10001`, so
minimum width does not truncate larger values. Three CLI tests passed. Parent
inspection checked the change and independently exercised a four-byte UTF-8
input: `🧪` produced `checksum=02f1`. The accepted process exited naturally in
88.712 seconds, emitted `turn.completed`, and left zero owned active processes;
peak Job memory was 139,608,064 bytes.

`first-acceptance.json`, `second-run/`, `second-final.txt`, and
`accepted-memory.patch` retain command/process evidence and the exact change.
An initial diff-export argument error was corrected by exporting the committed
fixture delta; it did not lose changes. The fixture result was committed only to
its owned repository as `a242ba7b7fe5d97c236aa208f442f0854e8619a9`.

## Fresh clone and no redundant record

`fresh-clone/` was cloned without hard links and used `clone-codex-home/`, which
had no project sessions before the run. A fresh Astra process received no prior
project conversation or checksum details. It discovered the Git memory route,
used the saved decision, checked all three recorded source fingerprints, and ran
the focused/full tests plus `cargo run -- AB`. All checks passed; Git stayed
clean. No new useful fact arose, so it made no memory write.

The process exited naturally in 58.089 seconds, with `turn.completed`, zero active
owned processes and peak Job memory of 137,236,480 bytes. See
`clone-baseline.json`, `clone-acceptance.json`, `clone-run/`, and `clone-final.txt`.
These bounded acceptance calls do not introduce a model call into normal memory
maintenance or establish a percentage subscription saving.

## Changed decision and conflicting worktrees

Two actual linked worktrees started from controlled fixture baseline
`12218634253691d1ce784c9301f8c70b70c4ffbd`. One retained an earlier full-suite
verification preference; the other recorded a later controlled user decision
to use the current focused CLI test. Both edited the same owning paragraph.
The first merge attempt lacked a fixture committer identity and made no merge;
the corrected command supplied identity locally, without global Git changes.

Git then produced an actual three-stage conflict. Both committed versions and
the conflict markers were retained. Parent integration applied the later
decision while preserving the useful missing-input coverage requirement from
the other version. The chosen command passed all three actual CLI tests in the
combined tree. The index stayed a link to the owning record, without a second
decision copy. Both worktree files stayed unchanged. Evidence is in
`memory-conflict-acceptance.json`, `memory-conflict-markers.md`, and the two
`memory-worktree-*-preserved.md` files; owned merge commit is
`d7a2958`.

## Connection lifecycle and scope

The existing core installer connected each owned home with 17 links, 12 skills
and three agent definitions and verified neutral CLI startup. Disconnect and
reinstall of the first owned home preserved all project memory, decisions,
verification, packaging and unrelated-file hashes, including the independent
authentication link. `memory-lifecycle.json` and lifecycle logs retain those
checks. The active global proxy and unrelated installations were not changed.

This closes Git-memory tasks 1.3 and 1.4. Separate
[ordinary-context runtime acceptance](native-context-delivery.md) now passes;
[overall skill lifecycle acceptance](native-workflow-lifecycle.md) also passes.
