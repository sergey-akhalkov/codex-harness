# Subscription routing verification

Status: global delivery complete on 2026-09-07; all 13 [implementation tasks](../../openspec/changes/archive/2026-09-08-connect-subscription-model-routing/tasks.md) are complete. The managed global proxy is running and ready. This record contains observations from Windows on 2026-09-06–07, including failed attempts and their later corrections. The user explicitly ended further investigation of the contained memory event; its cause is not claimed resolved.

## Actual subscription and contained runtime

- Independent xAI OAuth completed through the delivered browser-only entry. Job peak 329,527,296 bytes, elapsed 36,317 ms, exit 0; see the [memory incident record](subscription-memory-incident.md).
- Authenticated GET `https://cli-chat-proxy.grok.com/v1/models` returned HTTP 200 and exactly `grok-4.6`, `grok-4.5` at 20:23:19 UTC. The direct Chat Completions probe returned HTTP 200 and the requested control answer; xAI reported `grok-4.6-build` for requested `grok-4.6`.
- A finite, foreground [actual OpenCodex server probe](../../tests/subscription-proxy.Tests.mjs), with all native client integrations and sidecars disabled, returned HTTP 200 and `PROXY_SUBSCRIPTION_OK` through `/v1/responses`. Instrumented network evidence recorded `cli-chat-proxy.grok.com/v1/responses`, HTTP 200. Peak job memory 429,428,736 bytes, elapsed 12,784 ms, exit 0. The server was explicitly stopped; the job cleaned up descendants.
- A separate credential-free fixture returned HTTP 401 `authentication_error`/`invalid_api_key` without any outgoing requests. An unavailable-provider request also hit the authentication gate; this fixture alone does **not** prove authenticated unavailable-model behavior.
- Repository JSON passed the pinned package's `validateConfigCandidate`; role TOML parsed, required role fields and credential-field checks passed through [the source validator](../../tools/opencodex-config-check.mjs). Native role model precedence was inspected for Codex CLI 0.153.4 and confirmed by actual named-role consumption below.

Private evidence lives under `~/.codex/harness/runtime/subscription-probe-20260906-01/`: `authenticated-models.json`, `direct-subscription-completion.json`, `foreground-probe-dd74ec07ab7e42db8a9b4141b3085538/`, `failure-probe-bf197d2590694eb5b581bb4f2693a0c2/`. On completion of the isolated checks, the independent OAuth file was relocated to `~/.opencodex/auth.json`, without retaining another refresh owner. OpenCode credentials were never copied or refreshed.

## Deterministic lifecycle and review

- Windows process runner: **7 real sequential Node scenarios**, including aggregate memory exhaustion, timer expiry, supervisor termination, ordinary parent exit, assigned-before-resume receipt, and trusted observer exception cleanup.
- Subscription lifecycle: **139 assertions** passed on 2026-09-07 with actual temporary source links and mocked package/task/runtime effects. Coverage includes preview and Check purity, repeated installation, authentication preservation, interrupted/late failure recovery, concurrent foreign edits, deferred restart after the outer MCP restoration, exact role readiness, occupied-port native restoration, and stale process marker handling. COM missing-task lookup and task XML construction were read against Windows; these are not task-start evidence.
- Combined activation: **19 scenarios / 152 assertions**, including actual temporary links, the native Codex TOML editor, and crash checkpoints; subscription runtime effects were fixtures.
- Independent review found and corrected `/health` versus `/readyz`, foreign runtime ownership during native restoration, and missing role restoration after a later service start. A separate review identified the unbounded native consumer process tree; the acceptance probe now starts its actual global CLI inside a 2048 MiB Windows job.
- The 2026-09-07 focused independent review additionally found occupied-port startup bypassing native restoration (P1) and credential-bearing `apiKeys` passing source validation (P2). Both fixes passed focused tests and follow-up source review. This review found no further material issues in its inspected changes; the subsequent live memory-limit incident remains a separate unresolved acceptance failure.
- [Actual Windows I/O checks](../../tests/code-tools-io.Tests.ps1) prove that the journal reader can retain a complete old snapshot while the writer replaces the path, that a foreign non-sharing lock fails without damaging the journal, and that UTF-8 BOM and absent-file behavior remain supported. The observed `File.Move(overwrite)` failure also reproduced with a delete-sharing reader; `File.Replace` succeeded. The reader now shares deletion and existing files use replacement, consistent with the [Windows replacement contract](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-replacefilew). This resolves the observed pending-journal `Access denied` race without an unbounded retry.
- [Source validation](../../tests/subscription-config.Tests.ps1) passed three actual bounded Bun runs: valid source, rejected synthetic `apiKeys`, and rejected synthetic role token, with no sentinel in output. Test homes contained no account credentials. Private fixture: `%TEMP%/codex-subscription-config-d288bde6570043c9b6d05448648bf97f/`.
- Combined activation was rerun after the I/O change: **19 scenarios / 152 assertions** passed, including native TOML editing and hard-crash recovery. Private fixture: `%TEMP%/harness-activation-f293d035e9b5469599a70b4aff988490/`.
- Documentation validation on 2026-09-07 checked **68 local links and their Markdown anchors in 10 files** covering the changed guides, evidence and OpenSpec artifacts. Runtime claims were checked against the pinned package and relevant official Codex, xAI and Windows documentation; model availability remains scoped to the authenticated observation above.

## Initial global activation attempt

`install.ps1 -SubscriptionsOnly -Mode Install` failed the 30-second background readiness check. Only the pre-install source validator produced a bounded run log; no service-side validator or proxy run was observed. The installer removed its task and links and restored its prior state. SHA-256 comparison confirmed unchanged native Codex config, harness profile, OpenCode config and OpenCode auth.

The selected `pwsh` was the Microsoft Store executable. A separate native installation at `C:\Program Files\PowerShell\7\pwsh.exe` was found and executed successfully, reporting 7.6.5. The service launcher now prefers that installation and preserves early startup diagnostics. A second actual Install succeeded at 20:59:53 UTC, with the hidden task running and `/readyz` confirming the owned Bun PID on port 10100. The absence of a service log alone does not prove the precise Task Scheduler failure cause. Task Scheduler Operational logging was disabled and was not enabled by this work.

## Actual global consumer checks

The ordinary Codex Grok main-model run outside the checkout exited 0. It read the unique local fixture and returned the exact marker. The native session selected `xai/grok-4.6`, reasoning `high`. Two correlated proxy requests returned HTTP 200, each with one explicit xAI route and credential source `grok-oauth`; xAI reported `grok-4.6-build`.

GPT-6 Astra parent `01a07894-3e94-7ec0-b32d-7129fc07dfff` invoked the exact `grok_reviewer` role with `fork_context:false`. Child `01a07894-a863-75e1-9836-1bfd6748d044` selected `xai/grok-4.6`, reasoning `high`, read the child-only fixture through its shell, and returned its unique marker and the 41-versus-42 discrepancy. The parent did not read the fixture itself and relayed the child's findings. Both correlated child requests returned HTTP 200 with one xAI `grok-oauth` attempt. Parent CLI exit was 0; the actual launcher and descendants ran inside a 2048 MiB job.

The initial acceptance parser mishandled ru-RU date separators and CLI 0.153.4 completed-item events. [The corrected probe](../../tests/subscription-consumer.Tests.ps1) revalidated retained process results and rollouts, without another model request: **16 main checks and 24 delegation checks passed**. Original reports remain alongside distinct `revalidated-*.json` reports in private temporary folders `codex-subscription-consumer-fb102bec525a442da857b77da0e128af` (main) and `codex-subscription-consumer-198132cfaaef464587a0af06c159c1a4` (delegation). Redacted proxy correlations are in `main-proxy-trace.json` and `consumer-proxy-trace.json` under the probe root.

An authenticated request for a unique nonexistent `xai/codex-harness-unavailable-…` model returned HTTP 400 `Model not found`, with no output. The proxy recorded exactly one attempt, the original xAI model, and `grok-oauth`; no fallback occurred. Private evidence: `authenticated-unavailable-model.json` and `unavailable-model-proxy-trace.json` under the probe root.

Existing launcher regression passed **147 assertions**. Core installer regression passed **21 scenarios / 125 assertions**, including actual isolated native CLI startup. Independent review accepted the narrow service-entry, native PowerShell selection and deferred-restart changes; these source reviews are distinct from actual scheduler evidence.

## Lifecycle correction and final global acceptance

The new read-only global mode was exercised against the degraded installation. It reported failure immediately, preserved the native-config hash and left the service degraded; it did not reconnect. Private report: `%TEMP%/codex-subscription-lifecycle-f769f326873f4e218098e2e83be4853c/report.json`. The disruptive mode still rejects execution from Codex before any lifecycle effect.

The [isolated scheduler probe](../../tests/subscription-isolated.Tests.ps1) identified a separate false-negative HTTP check on 2026-09-06 at 23:21:34 UTC. The ordinary PowerShell request timed out after 3,025 ms; immediately afterward, `-NoProxy -DisableKeepAlive` returned ready in 86 ms and independent `curl --noproxy '*'` in 89 ms, for the same live PID 23924. TCP accepted connections, private memory was 314,331,136 bytes, and retained application/response state was zero. This distinguishes that missed deadline from process death; it does not isolate proxy discovery versus persistent-connection behavior individually. The production readiness request now uses both flags with its original two-second deadline, consistent with [PowerShell's documented parameters](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/invoke-restmethod?view=powershell-7.6). The 139-assertion component suite passed after this correction. The three diagnostic fixture tasks were removed, with all eight global file hash/absence comparisons preserved. Private comparison: `%TEMP%/codex-subscription-isolated-0994a38a268a42deb0afbe730d8e2d7a/http-comparison.jsonl`.

The final isolated lifecycle passed **28 checks**, using the real pinned package, Scheduler and production readiness code: Install, repeat Install, managed stop/Recover, task restart, Disconnect and reconnect. Thirteen samples over 120.79 seconds all confirmed the same ready process with the three HTTP clients. Native config bytes were restored three times, direct source links were checked four times, and all four service launches retained the 768 MiB job allowance. The fixture had no provider credentials and blocked external requests, so this establishes lifecycle behavior with those substitutions. Its task and processes were removed; all eight global file hash/absence checks passed, including the actual `~/.codex/harness.config.toml` profile. Private report: `%TEMP%/codex-subscription-isolated-b544f3b5c6ea44cbad26ebda632d7712/report.json`; the 180.141-second worker completed before the final global Install.

The 2026-09-07 live cycle passed **45 checks**: repeat Install; exact source links and task identity; actual managed task stop/restart and role restoration; exact native snapshot restoration; ordinary native CLI outside the checkout; discovery of Serena, Codebase Memory, Graphify, Nuphus and harness LSP after Disconnect; and unchanged OpenCode config/auth, harness profile and independent xAI authorization. Reconnect returned ready, but the immediate independent Check failed; the later managed run hit its 768 MiB job limit. This cycle is **failed**, not complete. Its finalizer's original reconnection label was insufficient and has been corrected to require Check. See the [incident and retained report](subscription-memory-incident.md#acceptance-session-disconnected-2026-09-07).

Native restoration in that cycle matched the exact snapshot captured from the prior routing journal (`native-cycle-baseline-e715a4a85a34400ab27086d3abd67e6d.toml`). That snapshot differs from the historical pre-first-install native-config hash; the test records both separately. Thus exact recovery is established for the cycle baseline, not byte identity to the older native config. OpenCode auth/config and the linked harness profile still matched their historical baselines at the tested checkpoints.

Final `install.ps1 -SubscriptionsOnly -Mode Install` returned ready. The read-only global acceptance then passed **38 checks**, from 23:36:56.735 to 23:39:01.954 UTC on 2026-09-06 (2026-09-07 Moscow). Thirteen samples over 124.77 seconds attested the same owned PID 780; the largest sampled private-memory value was 380,366,848 bytes. Exact source links, ordinary global CLI startup, heterogeneous v1 mode and all five existing MCP registrations were verified through native commands outside this checkout. OpenCode config/auth and the linked harness profile matched their historical hashes; independent xAI authorization remained unchanged. Private report: `%TEMP%/codex-subscription-lifecycle-5c3f392a9e654a65b9435715103e0697/report.json`. The service remains running after the probe.

Actual Grok main-model use, exact named-role delegation and unavailable-model rejection are established above; the final activation and isolated lifecycle complete the remaining delivery evidence. Focused independent review found no material issues in the final readiness transport or read-only probe. The global lifecycle test refuses to run from Codex; disruptive checks use isolated homes and their own port/task. Windows reboot has not been performed. Automatic workspace language diagnostics repeatedly timed out or returned stale/unavailable results, so no clean overall diagnostic result is claimed. Changed PowerShell files parsed and OpenSpec strict validation passed; these checks complement the exercised runtime paths.
