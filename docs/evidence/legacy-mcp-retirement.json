{
  "schema": 1,
  "capturedUtc": "2026-09-23T18:55:00Z",
  "change": "retire-codegraph-sharpen-serena",
  "task": "retirement record for CodeGraph and the final Codebase Memory removal",
  "liveMcp": [
    "serena",
    "nuphus"
  ],
  "retiredMcp": [
    "graphify",
    "codebase-memory",
    "codegraph",
    "harness-lsp"
  ],
  "registrationGuard": {
    "where": [
      "crates/harness-core/src/mcp_registration.rs (CBM, GRAPHIFY and GRAPH skip/conflict lists and all_names)"
    ],
    "rationale": "an Update must remove an owned registration recorded by a pre-retirement version instead of re-registering a broken server, and must still refuse name/ownership conflicts on an unowned same-name table",
    "removalCondition": "drop a name only when no supported host state can predate its retirement; revisit at the next major lifecycle change",
    "tests": [
      "crates/codex-harness/tests/mcp_registration.rs"
    ]
  },
  "codegraph": {
    "retired": "2026-09-23 from the managed selection and from first-party code by the user's decision",
    "evidence": [
      "measured Code Mode invocations over 30 local rollout days: Serena 689, Nuphus 662, CodeGraph 49, with 41 CodeGraph calls in the 2026-09-13..15 acceptance window and 0-3/day after 2026-09-18",
      "a live session returned 'CodeGraph client is not connected' for every managed codegraph_search while check --code-tools-only still reported the registration connected",
      "first-party maintenance surface was about 7.5k source and 5.5k test lines, roughly twice the Serena integration"
    ],
    "removedSurface": [
      "crates/harness-core/src/codegraph_*.rs serving, broker, scheduler, observer, runtime, catalogue, response, generation, store and transport modules",
      "crates/harness-core/src/dependency_codegraph.rs and every codegraph dispatch arm in dependency discovery, planning, staging, selection, audit and fetch",
      "codex-harness mcp codegraph, codegraph-control, retire-codegraph CLI routes; prepare-codegraph/apply-codegraph-registration renamed to prepare-mcp/apply-registration",
      "the codegraph mcp entry in global/code-tools.json and the CodeGraph resource row",
      "tests: codegraph_consumers, codegraph_dependency, codegraph_failures, codegraph_mcp, codegraph_transport, codegraph_generation and the codegraph comparison support"
    ],
    "hostResidue": "shared published package, saved graphs, .codegraph-harness-* caches and account/broker state stay on the host as inert data; the kit neither reads nor restores them"
  },
  "codebaseMemory": {
    "history": "retired from the managed selection 2026-09-17; the manual stdio/broker route and cbm_* modules remained as documented rollback residue",
    "removed": "2026-09-23 with the graph cleanup: cbm_* modules, the mcp codebase-memory CLI, cbm dependency planning/probing and the cbm_* tests are gone; the registration guard above still removes an owned legacy registration on Update"
  },
  "referenceSweep": {
    "commands": [
      "git grep -i codegraph",
      "git grep -i 'codebase-memory'",
      "git grep -i graphify"
    ],
    "allowlist": [
      "current change artifacts and OpenSpec archives (historical records)",
      "docs/evidence/rust-migration-map.json (dated inventory), rust-migration.md and project-decisions.md (decision history)",
      "retirement notes in docs/code-tools.md and this record",
      "the registration guard constants and their tests",
      "current specs that describe the accepted retired selection"
    ]
  }
}
