# Философия и решения проекта

[Карта документации](README.md)

Здесь собраны цели, ограничения и договорённости, явно заданные или подтверждённые пользователем. Это постоянный контекст для следующих сессий. Официальные рекомендации и предложения по реализации находятся в других документах и имеют свой статус.

Последнее обновление: **2026-09-07**. Основание — обсуждение назначения репозитория, философии, критерия завершения, глобальных принципов и Full Access, воспроизводимого развёртывания, глобальной поставки MCP/LSP, автоматической диагностики и подключения подписок.

## Назначение

`codex-harness` — переносимая основа глобальной настройки и расширения Codex CLI для разработки в других репозиториях. Польза набора оценивается по работе в целевых проектах.

Источник подходов для адаптации — `opencode-kit`. Из него переносим возможности, которые помогают достигать принятого результата быстрее и с нужным качеством.

**2026-09-07, подтверждённый запрос пользователя:** провести аудит дополнительных идей, кода, документации и инструкций `opencode-kit` для повышения результативности, качества и скорости. [Результат аудита](opencode-kit-audit.md) содержит проверенные наблюдения и предложения агента; состав следующей реализации этим запросом не выбран.

## Глобальная поставка по умолчанию

**2026-09-06, подтверждённое решение пользователя:** всё, что разрабатывается в этом репозитории, по умолчанию предназначено для использования за его пределами. Просьба добавить MCP, LSP или другую возможность означает глобальное подключение, чтобы она была доступна во всех сессиях Codex в любых проектах. Область только этого репозитория допустима по явному указанию пользователя.

Завершение такой задачи включает глобальную активацию и проверку работы вне `codex-harness`. Исходники и способ подключения должны оставаться частью переносимого набора и его [процесса установки](installation.md); наличия файлов или работоспособности только внутри checkout недостаточно.

## Результативность, качество и скорость

Все три свойства обязательны для оценки пользы набора:

- **Результативность:** довести согласованную задачу до завершения и получить требуемый результат. Скорость и качество имеют смысл при достигнутом результате.
- **Качество:** ориентироваться на отсутствие P0/P1 инцидентов и багов в получившихся фичах. Выявленные P0/P1 устранять до завершения работы; выбирать проверки по реальным сценариям и существенным рискам.
- **Скорость:** сокращать время до полного, проверенного результата в целевом репозитории, включая проверку и необходимые исправления.

Результат задаёт цель, качество — условия приёмки, скорость — предмет оптимизации. Набор инструментов, instructions, skills и агентов оценивается по его вкладу в эти цели.

## Основа — официальный Codex CLI

Основой остаётся официальный Codex CLI с глобальной конфигурацией, skills, агентами, плагинами, MCP и другими подходящими расширениями.

Собственный оркестратор, runtime или изменения самого CLI рассматриваем только при доказанной недостаточности штатных средств расширения для нужного результата. Это направление уточнено и больше не является открытым выбором исходной архитектуры.

## Уровни агентов и реальная эффективность

**2026-09-07, подтверждено пользователем:** цель — реальная экономия лимитов ChatGPT, ускорение полного проверенного результата и автономность. Делегирование оправданно только с учётом постановки, проверки и переделок; его количество не является метрикой успеха. Завершение и качество приоритетны, агент самостоятельно выбирает эффективный способ и обоснованно расходует лимиты.

Уровни универсальны: middle, senior и principal; деятельность определяется заданием и подходящими skills. Grok 4.6 middle всегда приоритетен для подходящего делегирования при доступности. GPT middle на подписке ChatGPT — резерв при недоступности Grok. Senior — Astra xhigh; principal — редкая Astra max консультация по сложному интеллектуальному блокеру. Прямое выполнение, резерв и обоснованная эскалация в согласованной задаче не требуют нового разрешения. `grok_reviewer` заменяется универсальным уровнем. Уточнение пользователя: для всех OpenAI-вызовов использовать только семейство Astra, без GPT-5.*, включая вспомогательные вызовы. Резервный middle — Astra high; приоритетный Grok 4.6 — xhigh (поддержка подтверждена документацией). Технические ограничения и доказательства — в [adaptive-agent-delegation](../openspec/changes/adaptive-agent-delegation/proposal.md); пользователь поручил создать спеку и сразу реализовать её.

**2026-09-07, повторное отключение при делегировании:** пользователь сообщил о рестарте сессии и предположил недостаточный предел Bun. Результат службы подтвердил остановку по памяти при 768 MiB; это наблюдение, не установленная причина роста. В рамках восстановления агент увеличил только предел службы до 2048 MiB с сохранением containment; административные команды остались на 768 MiB. Проверки и границы вывода — в [отчёте делегирования](evidence/agent-delegation.md). Это новый эпизод после прежнего решения прекратить исследование предыдущего инцидента.

**Наблюдение при приёмке:** на паре коротких функций принудительное делегирование двум Grok увеличило время и расход Astra-родителя. Политика уточнена агентом: объединять связанную рутину и не дробить мелкие задачи. Общая экономия недельной квоты не объявляется доказанной; числа и ограничения сравнения находятся в том же отчёте.

## Основной процесс — OpenSpec

**2026-09-07, подтверждённый запрос пользователя:** перепроверить рекомендации `docs/opencode-kit-audit.md`, исправить критичные ошибки, самостоятельно выбрать улучшения с максимальным ROI для первой реализации, создать и полностью реализовать спеку, заархивировать её, затем запушить **все** изменения репозитория. Агент выбрал диагностику источников через Check; это технический выбор внутри порученного объёма. [Спека и обоснование](../openspec/changes/archive/2026-09-07-diagnose-effective-harness-sources/proposal.md). Прочие кандидаты аудита не становятся обязательным планом.

Разработку, как правило, ведём через OpenSpec. Это процесс по умолчанию; формулировка пользователя допускает исключения в конкретных задачах.

Актуальная согласованная спека определяет требуемый результат и объём работы. Задачи описывают работу, необходимую для её выполнения.

## Воспроизводимое развёртывание из репозитория

**2026-09-06, подтверждённая цель пользователя:** этот репозиторий должен быть источником переносимой глобальной конфигурации и возможностей Codex, включая агентов и skills. На новом ПК пользователь хочет развернуть набор из checkout без повторной ручной настройки каждого компонента.

**Принятый объём первой реализации:** `install.ps1` для Windows native и PowerShell связывает checkout с уже установленными Codex CLI и OpenSpec. Установка внешних приложений и поддержка других ОС остаются отдельной работой. Состав, обновление и обратимое отключение описаны в [инструкции установки](installation.md).

**Подтверждённое уточнение пользователя:** Codex должен дополнительно читать файлы и каталоги непосредственно из этого checkout. Копирование артефактов в целевые каталоги и сборка отдельного конфига из их содержимого исключены. Подключение должно использовать ссылки или поддерживаемые дополнительные пути; существующие локальные возможности и машинные данные сохраняются. Предложение агента об установке с объединением отклонено.

**Подтверждённый состав:** репозиторий должен быть полностью переносимым конфигом и набором инструментов Codex. Полный источник глобального `AGENTS.md`, как и остальные релевантные управляемые артефакты и их ресурсы, должен храниться в этом репозитории. Внешние зависимости должны быть явно описаны; перенос не должен требовать незаписанных исходников со старого ПК. Это цель полноты выбранного набора, а не утверждение о реализации всех возможностей исследовательского каталога.

Пользователь поручил реализовать [link-global-codex-kit](../openspec/changes/archive/2026-09-06-link-global-codex-kit/proposal.md) и архивировать после выполнения всей спеки. Установщик и связанный launcher реализованы и активированы на текущем ПК: 10 связей, шесть skills, полный глобальный текст инструкций и общий профиль с Full Access. Реальный запуск вне harness и совместная загрузка проектных настроек прошли [проверку приёмки](linked-kit-verification.md). Все 17 задач выполнены, 9 требований и 22 сценария синхронизированы с [основной спекой](../openspec/specs/linked-global-kit/spec.md); change архивирован 2026-09-06. Каталог агентов подключён, производственных ролей в нём пока нет. Авторизация и состояние сессий остаются вне репозитория.

**2026-09-06, подтверждённое решение пользователя:** машинные записи в общем конфиге допустимы, включая пути доверенных проектов. Штатный TUI `/model` и подтверждение доверия могут записывать настройки через ссылку прямо в исходник checkout. Это принятое поведение, прежнее требование держать такие настройки исключительно локально снято. Авторизация, история, кэши и метаданные установщика остаются вне репозитория. Сохраняем официальный Codex CLI; собственная сборка не требуется. Спека и [отчёт приёмки](linked-kit-verification.md#tui-и-сохранение-настроек) учитывают это решение.

## Глобальные MCP, LSP и автоматическая диагностика

**2026-09-06, подтверждённые требования пользователя:** подключить глобально четыре MCP из `opencode-kit` — Serena, Codebase Memory, Graphify и Nuphus — и поддержку LSP только для Rust, TypeScript, JavaScript, PowerShell, Python, Delphi, C++, C#, JSON, Markdown, TOML, XML, CMake и Bash. Инструменты должны быть доступны в сессиях Codex во всех проектах. Существующие установки предпочтительно переиспользовать; повторная установка тех же инструментов для Codex нежелательна. При наличии новых версий пользователь хочет обновить общие установки с проверкой работоспособности.

Пользователь подтвердил **автоматическую диагностику после правок** и поручил реализацию `$openspec-apply-change connect-global-mcp-lsp`. Полный каталог исходного kit исключён. После [инвентаризации расширений](language-inventory.md) в `\\PC\mekha` подтверждён приведённый выше обязательный набор; YAML, QML, HTML и CSS подключаются при наличии готовой совместимой поддержки. C# по словам пользователя уже установлен. Специальные PMAC/ЧПУ-форматы, KDL и другие неподтверждённые языки пока не включены.

Нормативные требования — в [основных спеках](../openspec/specs/), технические решения и проверки — в [архиве изменения](../openspec/changes/archive/2026-09-06-connect-global-mcp-lsp/proposal.md). **Фактическая поставка 2026-09-06:** глобальное подключение и реальные вызовы четырёх MCP проверены вне harness; четырнадцать обязательных языков и доступные HTML/CSS прошли автоматическое введение/снятие ошибки. YAML/QML явно недоступны. Все 45 задач выполнены. По запросу пользователя change архивирован 2026-09-06; три новые основные спеки и два требования `linked-global-kit` синхронизированы без потери прежних сценариев. [Отчёт приёмки](code-tools-verification.md) содержит проверки совместимости с OpenCode, установки и переноса, ограничения заменителя отдельного ПК и обоснованно удержанные версии PSES/Rust. Это результат реализации, а не расширение пользовательского языкового набора.

Эта поставка расширяет установщик: прежнее ограничение первой реализации «не устанавливает и не обновляет внешние приложения» не является ограничением MCP/LSP-задачи. Адаптеры, транспорт и политика совместимости описаны как решения реализации, а не отдельные пользовательские предпочтения.

**2026-09-07, подтверждённый запрос пользователя:** сразу исправить частые таймауты хуков, в том числе наблюдаемый лимит 10 секунд. Исправление сохраняет автоматическую диагностику, конечные сроки ожидания и честный статус неполных проверок. Причины, глобальная проверка и граница обновления уже запущенных MCP описаны в [приёмке исправления](evidence/hook-timeouts.md); задачи — в [fix-diagnostic-hook-timeouts](../openspec/changes/fix-diagnostic-hook-timeouts/tasks.md).

**2026-09-07, подтверждённый запрос пользователя:** исправить повторяющиеся `Blocked by hook` и одинаковые сообщения Stop, вызванные сбоем Markdown на ссылках в соседний checkout. Исправление должно работать глобально и сохранять честную диагностику; задачи — в [fix-markdown-stop-hooks](../openspec/changes/fix-markdown-stop-hooks/tasks.md), причины и глобальная приёмка — в [отчёте исправления](evidence/markdown-stop-hooks.md).

## Подписки и модели внешних провайдеров

**2026-09-06, подтверждённое решение пользователя:** реализовать согласованный план глобального подключения OpenCodex к официальному Codex CLI, чтобы использовать подписки внешних провайдеров и назначать конкретные модели именованным субагентам. Первая подписка — **SuperGrok Heavy**, по словам пользователя уже работающая в OpenCode. Существующий OpenCode необходимо сохранить. Главный агент остаётся GPT-6 Astra; начальная внешняя роль — Grok reviewer, смешанное делегирование использует v1. Реализация — [connect-subscription-model-routing](../openspec/changes/connect-subscription-model-routing/proposal.md). Другие подписки пока не названы.

При первом OAuth пользователь сообщил об исчерпании памяти и завершил Bun. [Запись инцидента](evidence/subscription-memory-incident.md) разделяет наблюдения, воспроизведённый дефект закрытого stdin и непроверенную атрибуцию PID. Перед дальнейшим запуском реализация добавляет browser-only OAuth и проверяемый лимит памяти ОС; это техническое исправление в рамках исходного требования предотвращать P0/P1.

**2026-09-07, обратная связь пользователя:** приёмочный тест остановил прокси текущей сессии; после длительного ожидания переподключения пользователь перезапустил Codex. Обрыв и дальнейший memory-limit зафиксированы в [записи инцидента](evidence/subscription-memory-incident.md#acceptance-session-disconnected-2026-09-07). Техническое исправление агента: проверки остановки и восстановления изолируются от канала управления текущей сессией; глобальный destructive probe из Codex запрещён.

**2026-09-07, подтверждённое указание пользователя:** прекратить дальнейший разбор последнего сбоя памяти и завершить спеку подключения подписок. Пользователь допускает разовый характер события; это предположение, а не установленная причина. Лимит памяти и восстановление сохраняются. Дополнительное воспроизведение и точная атрибуция этого события не являются условием завершения текущей работы.

**Фактическая поставка 2026-09-07:** все 13 задач выполнены; глобальная служба готова, Grok проверен как основная модель и именованный субагент GPT. Пройдены восстановление, перезапуск и переподключение, существующие MCP и OpenCode сохранены. [Отчёт приёмки](evidence/subscription-routing-verification.md) разделяет реальные глобальные проверки, изолированный lifecycle и ограничения доказательств. Инструкция — [подписки и модели](subscription-models.md).

## Когда задача или фича завершена

Критерий пользователя: **«выполнена вся спека (все задачи закрыты)»**.

Для работы по OpenSpec завершение означает выполнение всей актуальной согласованной спеки и закрытие всех задач соответствующего изменения. Закрытие задачи отражает фактически выполненную работу и предусмотренные проверки. Оставшиеся требования или незакрытые задачи означают, что работа ещё не завершена.

Для возможностей этого набора критерий завершения по умолчанию включает [глобальную поставку](#глобальная-поставка-по-умолчанию). Другие действия поставки — например, merge или публикация во внешнем сервисе — входят в критерий завершения, когда они предусмотрены согласованной спекой и задачами.

## Перенос и глобальная загрузка принципов

Пользователь поручил адаптировать `opencode-kit/global/principles-of-work.md` с опорой на собранную официальную документацию. Противоречия результативности, качеству и скорости нужно показывать и разрешать совместно.

Принципы должны входить в начальный контекст Codex во всех репозиториях через глобальные инструкции. Канонический переносимый текст хранится в [global/principles-of-work.md](../global/principles-of-work.md); [фактическое подключение](global-instructions.md) описано отдельно от философии.

После обсуждения обязательного свежего ревью, production-потребителя и фиксированного числа повторов пользователь подтвердил: **«Да, адаптируем правила по спеке и рискам»**. Дополнительные проверки определяются согласованными требованиями и конкретными рисками, особенно P0/P1. Число ревью и повторов зависит от прогресса и полученных данных. Production-проверка необходима для согласованного результата или соответствующего заявления о готовности.

Сравнение с оригиналом и причины замен сохранены в [карте адаптации](principles-port.md).

## Глобальный режим разрешений

**2026-09-06:** пользователь поручил включить **Full Access по умолчанию для всех сессий**, чтобы не выбирать его вручную. В пользовательском `~/.codex/config.toml` установлены `approval_policy = "never"` и `sandbox_mode = "danger-full-access"`; сочетание соответствует [официальному описанию Full Access](https://learn.chatgpt.com/docs/sandboxing#configure-defaults).

На текущей Windows-машине файл находится в `C:\Users\noilw\.codex\config.toml`; исходная копия сохранена рядом как `config.toml.bak-full-access-20260906-145605`. Загрузка обоих значений проверена через `codex debug prompt-input` в CLI 0.153.4 без флагов переопределения разрешений. Это глобальные значения для новых локальных сессий; явные параметры запуска и проектные настройки имеют [более высокий приоритет](https://learn.chatgpt.com/docs/config-file/config-basic#configuration-precedence). Машинный конфиг и его резервная копия остаются вне репозитория.

**2026-09-07:** пользователь попросил проверить возможность максимально доступных разрешений. Повторная проверка CLI 0.153.4 через `codex -C $env:TEMP debug prompt-input` без переопределения разрешений подтвердила `danger-full-access`, `never` и включённую сеть вне репозитория. Команды текущей сессии выполняются с повышенными правами администратора Windows; создание, чтение и удаление временного файла вне проекта и HTTPS-запрос из shell прошли. Дополнительного более широкого режима песочницы не требуется. Права аккаунтов внешних сервисов и отдельные [настройки инструментов приложений и MCP](https://learn.chatgpt.com/docs/config-file/config-reference) проверяются для конкретного подключения; эта проверка не подтверждает доступ ко всем внешним ресурсам. Настройки доступа не менялись.

## Как сохранять дальнейшие договорённости

Пользователь попросил постоянно фиксировать свои мысли о проекте, чтобы сохранять контекст между сессиями.

- В ходе обсуждения сохранять устойчивые цели, ограничения, предпочтения и подтверждённые решения в этом документе.
- Пользовательские идеи, которые ещё обсуждаются, явно отмечать как предварительные; предложения агента сохранять с их собственным статусом.
- При уточнении решения обновлять его текущую формулировку и связанные документы, убирая устаревшие открытые вопросы.
- Сохранять краткую суть и дату изменения; для задач конкретного изменения использовать его OpenSpec-артефакты.
- Актуальные указания пользователя определяют дальнейшее обновление этой записи.

Нерешённые вопросы о платформах, установке, составе расширений и расходе перечислены в [карте документации](README.md#что-ещё-нужно-решить).

Реализация первого пакета по аудиту (2026-09-07): глобальная диагностика источников подключена и проверена вне checkout. У CLI 0.153.4 нет app-server экспорта выбранного file-profile, поэтому вычисленные по native-слоям значения явно отделены от прямых наблюдений; неопределённый runtime остаётся `incomplete`/`unknown`. Это установленное ограничение реализации, а не изменение пользовательского критерия полного результата. [Контракт и приёмка](source-diagnostics.md).
